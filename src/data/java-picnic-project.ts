import { javaPicnicProjectManifest } from './java-picnic-project-manifest'
import type { GuidedProject, GuidedProjectCheckpoint, ProjectTestCase } from './project-types'

export type JavaPicnicProjectCheckpoint = GuidedProjectCheckpoint
export type JavaPicnicProject = GuidedProject

const finalProjectVisibleTestCase: ProjectTestCase = {
  id: 'final-visible-ten-guests',
  name: 'A ten-person picnic',
  visibility: 'visible',
  stdin: 'Alex Kim\n10\n',
  expectedStdout: [
    'What is your name?',
    'How many guests are coming?',
    'Table: Large',
    'Supply: Blankets',
    'Supply: Cups',
    'Supply: Napkins',
    'Picnic: Alex Kim | Guests: 10',
  ].join('\n'),
  purpose: 'Shows one complete large-table plan before the official check uses other organizer details.',
}

export const javaPicnicProject: JavaPicnicProject = {
  ...javaPicnicProjectManifest,
  checkpoints: [
    {
      id: 'project-java-build-path',
      order: 1,
      title: 'Follow Java from source to picnic',
      objective: 'Recognize every supplied part of the Java program frame and follow source through javac, bytecode, and the JVM before editing code.',
      scaffolding: 'guided',
      newTerms: [
        { term: '.java source file', meaning: 'A human-readable text file containing Java instructions, with a name ending in the .java extension.' },
        { term: 'javac', meaning: 'The Java compiler command that checks source code and translates accepted source into bytecode.' },
        { term: 'bytecode', meaning: 'Portable instructions produced by javac and stored in a .class file for a Java Virtual Machine to run.' },
        { term: 'JVM', meaning: 'The Java Virtual Machine, which carries out compiled Java bytecode on the current computer.' },
        { term: 'class', meaning: 'A named Java container that groups related methods and other program parts between its outer braces.' },
        { term: 'entry point', meaning: 'The method where a running program begins, written here as public static void main(String[] args).' },
        { term: 'case-sensitive', meaning: 'A rule that uppercase and lowercase letters are different, so Main and main are different Java names.' },
        { term: 'semicolon', meaning: 'The punctuation mark that closes many complete Java instructions inside a method body.' },
        { term: 'braces', meaning: 'The { and } symbols that open and close related groups such as a class, method, loop, or decision route.' },
      ],
      exercise: {
        id: 'project-java-build-path',
        conceptId: 'project-java-compile-run-cycle',
        eyebrow: 'Checkpoint 1 of 12',
        title: 'Follow Java from source to picnic',
        explanation:
          'Java begins as readable source saved in Main.java. public class Main opens the named program container, and its braces hold the program methods. public static void main(String[] args) is the starting method: public makes it reachable, static lets Java start it without first creating a Main object, void says it returns no value, and String[] args can receive launch words. Java is case-sensitive, parentheses and brackets must stay paired, braces group related code, and a semicolon closes many complete instructions. The javac compiler checks those details and produces Main.class bytecode. The JVM then runs that bytecode. You will not edit this supplied frame yet.',
        analogy:
          'A picnic plan begins as a readable page. A careful coordinator checks the page and turns it into a standard task card. Any trained picnic crew can follow that card. Main.java is the page, javac is the coordinator, bytecode is the task card, and the JVM is the crew carrying it out.',
        type: 'choice',
        prompt: 'Which path correctly describes how the supplied Java program reaches the screen?',
        choices: [
          { id: 'a', label: 'Main.java, javac, bytecode, JVM', detail: 'javac checks the source and produces bytecode that the JVM can run.' },
          { id: 'b', label: 'JVM writes Main.java for you', detail: 'The programmer writes the source. The JVM runs compiled bytecode instead of inventing source.' },
          { id: 'c', label: 'Semicolons replace the compiler', detail: 'Punctuation helps source pass a compiler check, but punctuation does not run the program itself.' },
        ],
        correctChoice: 'a',
        hint: 'Begin with the human-readable .java file, then choose the path that ends with the JVM running compiled bytecode.',
        recap: 'Main.java holds readable source, javac checks its exact Java spelling and punctuation, Main.class holds bytecode, and the JVM runs that bytecode.',
        xp: 8,
      },
    },
    {
      id: 'project-java-output',
      order: 2,
      title: 'Welcome the organizer',
      objective: 'Use the supplied Main class and main entry point to display one complete welcome line with Java console output.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'System.out.println', meaning: 'The Java output method used here to display one value and then move to a fresh console line.' },
        { term: 'method call', meaning: 'An instruction that asks a named method to run, with any supplied values placed inside parentheses.' },
        { term: 'string literal', meaning: 'Fixed text written directly in source code between a matching pair of double quotation marks.' },
      ],
      exercise: {
        id: 'project-java-output',
        conceptId: 'project-java-console-output',
        eyebrow: 'Checkpoint 2 of 12',
        title: 'Welcome the organizer',
        explanation:
          'The supplied public class Main and main method form the program frame you just examined. Inside main, System.out.println receives the quoted welcome text in its parentheses, displays that exact text, and moves the next output to a new line. The semicolon finishes the instruction.',
        analogy:
          'The picnic welcome table has a small announcement board. System.out.println is its post-one-line control, and the quoted message inside the parentheses is the note that every arriving organizer sees.',
        type: 'code',
        prompt: 'Replace the blank with the Java method call that displays Picnic planner is ready.',
        starterCode: 'public class Main {\n    public static void main(String[] args) {\n        _____("Picnic planner is ready");\n    }\n}',
        focus: 'Replace only the _____ blank with System.out.println. Keep the supplied class, main method, parentheses, text, quotation marks, and semicolon.',
        codeGuide: [
          { code: 'public class Main', plain: 'This supplied line opens the required class named Main, matching the Main.java file that javac receives.' },
          { code: 'public static void main(String[] args)', plain: 'This supplied entry point is where the JVM begins the program, and you do not need to rewrite or memorize it now.' },
          { code: 'System.out.println', plain: 'Capitalization matters: System begins with a capital S, while out and println use lowercase letters.' },
          { code: '("Picnic planner is ready")', plain: 'Parentheses hold the value passed to println, and double quotation marks identify the value as fixed text.' },
          { code: ';', plain: 'The semicolon marks the end of this complete Java output instruction inside the main method braces.' },
        ],
        checks: [
          { pattern: 'System\\.out\\.println\\s*\\(\\s*"Picnic planner is ready"\\s*\\)\\s*;', message: 'Use System.out.println with the exact supplied welcome message inside its parentheses.' },
        ],
        output: 'Picnic planner is ready',
        hint: 'The completed instruction begins System.out.println and keeps the supplied message inside its parentheses.',
        recap: 'System.out.println displays one complete Java console line from inside main, and the final semicolon closes the instruction.',
        xp: 10,
      },
    },
    {
      id: 'project-java-name-input',
      order: 3,
      title: 'Read a complete name',
      objective: 'Create one Scanner connected to console input and store a complete organizer name, including spaces, in a String variable.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'import', meaning: 'A source instruction that makes a Java class from another package available under its short name.' },
        { term: 'Scanner', meaning: 'A Java class used here to read text arriving through the standard console input stream.' },
        { term: 'object', meaning: 'A usable value created from a class, such as the scanner that reads this program input.' },
        { term: 'variable', meaning: 'A named place where a program stores a value so later instructions can retrieve that value.' },
        { term: 'String', meaning: 'The Java type used here to store a sequence of text characters, including a name with spaces.' },
        { term: 'nextLine', meaning: 'A Scanner method that waits for one complete input line and returns its text when Enter is pressed.' },
      ],
      exercise: {
        id: 'project-java-name-input',
        conceptId: 'project-java-scanner-line-input',
        eyebrow: 'Checkpoint 3 of 12',
        title: 'Read a complete name',
        explanation:
          'import java.util.Scanner makes the Scanner class available. new Scanner(System.in) creates one scanner object connected to console input. scanner.nextLine() waits for a complete line, including internal spaces, and returns that text so the String variable guestName can store it.',
        analogy:
          'A picnic greeter holds one clipboard connected to the sign-in line. The clipboard is the Scanner object, nextLine records the whole written response, and guestName labels the box where that response is kept.',
        type: 'code',
        prompt: 'Complete the guestName declaration so it reads the organizer name as one complete line.',
        starterCode: 'import java.util.Scanner;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        System.out.println("What is your name?");\n        String guestName = _____;\n        System.out.println("Organizer: " + guestName);\n    }\n}',
        focus: 'Replace only the _____ blank with scanner.nextLine(). Keep the import, Scanner setup, String type, variable name, equals sign, and semicolon.',
        codeGuide: [
          { code: 'import java.util.Scanner;', plain: 'This line makes the Scanner class available before the Main class begins, and its semicolon closes the import.' },
          { code: 'Scanner scanner', plain: 'The class name supplies the object type, while lowercase scanner becomes the local variable holding that object.' },
          { code: 'new Scanner(System.in)', plain: 'new creates the Scanner object, and System.in connects it to text supplied through the console.' },
          { code: 'String guestName', plain: 'The capitalized String type marks this storage as text, and guestName describes the value being stored.' },
          { code: 'scanner.nextLine()', plain: 'This method call reads all characters on the next input line, so a first and last name stay together.' },
        ],
        checks: [
          { pattern: 'String\\s+guestName\\s*=\\s*scanner\\.nextLine\\s*\\(\\s*\\)\\s*;', message: 'Read one complete line with scanner.nextLine() and store it in guestName.' },
        ],
        output: 'What is your name?\nOrganizer: Alex Kim',
        hint: 'The right side of the declaration is the scanner variable followed by the nextLine method call and empty parentheses.',
        recap: 'One Scanner object reads console input, nextLine returns a complete line, and the String variable guestName stores that returned text.',
        xp: 14,
      },
      practiceStdin: 'Alex Kim\n',
    },
    {
      id: 'project-java-number-input',
      order: 4,
      title: 'Turn guest digits into a number',
      objective: 'Read the next complete input line and parse its digit text into an int that Java can compare as a guest count.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'int', meaning: 'The Java type used here to store a whole number without surrounding quotation marks.' },
        { term: 'parse', meaning: 'To examine text that represents a value and convert that text into the requested data type.' },
        { term: 'Integer.parseInt', meaning: 'A Java method that converts suitable digit text, such as "10", into the integer value 10.' },
        { term: 'nested call', meaning: 'A method call placed inside another call so its returned value becomes the outer call input.' },
      ],
      exercise: {
        id: 'project-java-number-input',
        conceptId: 'project-java-integer-parsing',
        eyebrow: 'Checkpoint 4 of 12',
        title: 'Turn guest digits into a number',
        explanation:
          'scanner.nextLine() returns text even when every typed character is a digit. Integer.parseInt receives that returned text and converts suitable digits into an int. The nested calls run from the inside outward, and guestCount stores the resulting whole number.',
        analogy:
          'A sign-up sheet can contain the characters 1 and 0 while still being paper text. Parsing is the coordinator who reads those characters and records the actual number ten in the picnic count.',
        type: 'code',
        prompt: 'Complete the guestCount declaration so it reads digit text and parses that text into an int.',
        starterCode: 'import java.util.Scanner;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        System.out.println("How many guests are coming?");\n        int guestCount = _____;\n        System.out.println("Guests recorded: " + guestCount);\n    }\n}',
        focus: 'Replace only the _____ blank with Integer.parseInt(scanner.nextLine()). Keep the prepared int type, variable name, equals sign, and semicolon.',
        codeGuide: [
          { code: 'scanner.nextLine()', plain: 'The inner call receives the next complete input line as text, even when that line contains only digits.' },
          { code: 'Integer', plain: 'This capitalized Java class supplies useful operations related to whole-number int values.' },
          { code: '.parseInt(...)', plain: 'This method receives suitable digit text and returns the whole-number value represented by those characters.' },
          { code: 'int guestCount', plain: 'This declaration creates whole-number storage so later conditions can compare the guest count numerically.' },
          { code: 'inside before outside', plain: 'Java gets text from nextLine first, then passes that returned text into the surrounding parseInt call.' },
        ],
        checks: [
          { pattern: 'int\\s+guestCount\\s*=\\s*Integer\\.parseInt\\s*\\(\\s*scanner\\.nextLine\\s*\\(\\s*\\)\\s*\\)\\s*;', message: 'Use Integer.parseInt around scanner.nextLine() so guestCount receives an integer.' },
        ],
        output: 'How many guests are coming?\nGuests recorded: 10',
        hint: 'Begin the right side with Integer.parseInt(, put scanner.nextLine() inside, then close both method calls.',
        recap: 'Scanner returns input as text. Integer.parseInt converts suitable digit text into the int value that guestCount stores.',
        xp: 16,
      },
      practiceStdin: '10\n',
    },
    {
      id: 'project-java-summary-line',
      order: 5,
      title: 'Print a personal picnic summary',
      objective: 'Join fixed labels with two stored Java values to produce one readable organizer and guest-count summary line.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'concatenation', meaning: 'The act of joining text pieces and stored values into one longer string for display.' },
        { term: '+ operator', meaning: 'The Java symbol used here between text and variables to join them from left to right.' },
      ],
      exercise: {
        id: 'project-java-summary-line',
        conceptId: 'project-java-string-concatenation',
        eyebrow: 'Checkpoint 5 of 12',
        title: 'Print a personal picnic summary',
        explanation:
          'When a Java + has text on either side, it joins the pieces into a longer String. The fixed labels keep their quotation marks. guestName and guestCount stay outside quotation marks so Java retrieves their current values before println displays the finished summary.',
        analogy:
          'A picnic label maker feeds four pieces into one strip: a fixed heading, the organizer record, another fixed label, and the guest count. Each plus sign joins the next piece to the growing label.',
        type: 'code',
        prompt: 'Replace the blank with one concatenated expression that prints the organizer and guest count in the requested format.',
        starterCode: 'public class Main {\n    public static void main(String[] args) {\n        String guestName = "Alex Kim";\n        int guestCount = 10;\n\n        System.out.println(_____);\n    }\n}',
        focus: 'Replace only the _____ blank with "Picnic: " + guestName + " | Guests: " + guestCount. Keep both variable names outside quotation marks.',
        codeGuide: [
          { code: '"Picnic: "', plain: 'This quoted String is the fixed first label, including the space that should appear before the name.' },
          { code: '+ guestName', plain: 'The plus joins the current organizer text from guestName instead of printing the variable label itself.' },
          { code: '+ " | Guests: "', plain: 'The next fixed String adds a readable separator and the label for the numeric value.' },
          { code: '+ guestCount', plain: 'The final plus joins the current integer value, which Java represents as visible digits in the String.' },
          { code: 'System.out.println(...)', plain: 'println receives the completed concatenated String and displays it as one console output line.' },
        ],
        checks: [
          { pattern: 'System\\.out\\.println\\s*\\(\\s*"Picnic: "\\s*\\+\\s*guestName\\s*\\+\\s*" \\| Guests: "\\s*\\+\\s*guestCount\\s*\\)\\s*;', message: 'Join the exact Picnic and Guests labels with guestName first and guestCount second.' },
        ],
        output: 'Picnic: Alex Kim | Guests: 10',
        hint: 'Start with the quoted Picnic label, then alternate plus signs, variable names, and the quoted Guests label.',
        recap: 'Java concatenation uses + to join fixed text with stored values, while unquoted variable names retrieve the values to display.',
        xp: 16,
      },
    },
    {
      id: 'project-java-array',
      order: 6,
      title: 'Store the picnic supplies',
      objective: 'Create a String array that keeps the three picnic supply names together in a predictable written order.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'array', meaning: 'One variable that stores an ordered group of values sharing the same declared Java type.' },
        { term: 'array item', meaning: 'One stored value inside an array, such as the Blankets String in the supplies array.' },
        { term: 'index', meaning: 'The numbered position used to retrieve an array item, starting with zero for the first item.' },
        { term: 'initializer', meaning: 'The brace-enclosed values that fill a new array when its variable is first declared.' },
      ],
      exercise: {
        id: 'project-java-array',
        conceptId: 'project-java-string-array',
        eyebrow: 'Checkpoint 6 of 12',
        title: 'Store the picnic supplies',
        explanation:
          'String[] means an array whose items are Strings. The initializer places Blankets, Cups, and Napkins into supplies in that order. Index 0 retrieves Blankets because Java begins array positions at zero, while commas separate the three stored items.',
        analogy:
          'A packing checklist keeps several supply labels on one ordered page. The array is the whole page, each String is one line, and an index points to one exact position on that page.',
        type: 'code',
        prompt: 'Complete the supplies declaration with a String array containing Blankets, Cups, and Napkins in that order.',
        starterCode: 'public class Main {\n    public static void main(String[] args) {\n        String[] supplies = _____;\n        System.out.println("First supply: " + supplies[0]);\n    }\n}',
        focus: 'Replace only the _____ blank with { "Blankets", "Cups", "Napkins" }. Keep String[], supplies, the equals sign, and the semicolon.',
        codeGuide: [
          { code: 'String[]', plain: 'The square brackets after String tell Java that supplies will hold several text values instead of one.' },
          { code: 'supplies', plain: 'This plural variable name labels the complete ordered collection for later loop instructions.' },
          { code: '{ ... }', plain: 'These braces form the initializer that supplies every array item when the variable is declared.' },
          { code: '"Blankets", "Cups", "Napkins"', plain: 'Commas separate the three String items, and their written order becomes their array order.' },
          { code: 'supplies[0]', plain: 'Square brackets request one item by index, and zero retrieves Blankets from the first position.' },
        ],
        checks: [
          { pattern: 'String\\s*\\[\\s*\\]\\s+supplies\\s*=\\s*\\{\\s*"Blankets"\\s*,\\s*"Cups"\\s*,\\s*"Napkins"\\s*\\}\\s*;', message: 'Initialize supplies with the exact Strings Blankets, Cups, and Napkins in that order.' },
        ],
        output: 'First supply: Blankets',
        hint: 'Use one brace pair after the equals sign and separate the three quoted supply names with commas.',
        recap: 'A String[] array keeps several text items under one name. Its initializer fixes their order, and index 0 retrieves the first item.',
        xp: 16,
      },
    },
    {
      id: 'project-java-table',
      order: 7,
      title: 'Choose the table size',
      objective: 'Write an if and else decision that compares the stored guest count with the eight-person large-table threshold.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'condition', meaning: 'A true-or-false question that a program evaluates before choosing which instructions to run.' },
        { term: 'if statement', meaning: 'A Java decision that runs its first brace group only when the condition is true.' },
        { term: 'else', meaning: 'The alternate route that runs when the condition attached to the related if is false.' },
        { term: 'comparison operator', meaning: 'A symbol pair that compares values, such as >= for greater than or equal to.' },
        { term: 'branch', meaning: 'One possible route through a decision, represented here by one brace-enclosed group.' },
      ],
      exercise: {
        id: 'project-java-table',
        conceptId: 'project-java-if-else',
        eyebrow: 'Checkpoint 7 of 12',
        title: 'Choose the table size',
        explanation:
          'The condition guestCount >= 8 asks whether the stored count is at least eight. When the answer is true, Java runs the first brace group. Otherwise, else selects the second brace group. Exactly one table-size line reaches the console.',
        analogy:
          'A picnic setup card says groups of eight or more need the large table. The coordinator checks one guest count, then places exactly one of two table signs beside the reserved area.',
        type: 'code',
        prompt: 'Replace the comment with a complete if and else decision that prints Large for at least eight guests and Small otherwise.',
        starterCode: 'public class Main {\n    public static void main(String[] args) {\n        int guestCount = 10;\n\n        // Choose and print the correct table size.\n    }\n}',
        focus: 'Write the complete if (guestCount >= 8) and else structure. There is no _____ blank, and each branch should contain one System.out.println call.',
        codeGuide: [
          { code: 'if (guestCount >= 8)', plain: 'The parentheses hold a true-or-false comparison asking whether the stored count reached the large-table threshold.' },
          { code: '{ ... }', plain: 'The first braces group the one output instruction that runs only when the condition is true.' },
          { code: 'System.out.println("Table: Large");', plain: 'This first-branch instruction labels a picnic whose guest count is eight or greater.' },
          { code: 'else', plain: 'This keyword introduces the alternate route used only when the original comparison is false.' },
          { code: 'System.out.println("Table: Small");', plain: 'This second-branch instruction labels a picnic whose guest count is below eight.' },
        ],
        checks: [
          { pattern: 'if\\s*\\(\\s*guestCount\\s*>=\\s*8\\s*\\)\\s*\\{[^}]*System\\.out\\.println\\s*\\(\\s*"Table: Large"\\s*\\)\\s*;[^}]*\\}\\s*else\\s*\\{[^}]*System\\.out\\.println\\s*\\(\\s*"Table: Small"\\s*\\)\\s*;[^}]*\\}', message: 'Use if (guestCount >= 8) for Large and else for Small, with one exact output line in each brace group.' },
        ],
        output: 'Table: Large',
        hint: 'Begin with if (guestCount >= 8), put the Large output in its braces, then add else with the Small output.',
        recap: 'A Java if statement runs its first branch when the condition is true, while else supplies the alternate branch for a false result.',
        xp: 18,
      },
    },
    {
      id: 'project-java-foreach',
      order: 8,
      title: 'List every supply',
      objective: 'Use an enhanced for loop to visit every String in the supplies array and print one line for each current supply.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'loop', meaning: 'A programming structure that repeats a group of instructions instead of requiring copied lines.' },
        { term: 'enhanced for loop', meaning: 'A Java loop that visits each value in an array or similar collection from beginning to end.' },
        { term: 'loop variable', meaning: 'The temporary name that receives the current array item during one pass through a loop.' },
        { term: 'iteration', meaning: 'One complete pass through a loop body while it handles one current collection item.' },
      ],
      exercise: {
        id: 'project-java-foreach',
        conceptId: 'project-java-enhanced-for',
        eyebrow: 'Checkpoint 8 of 12',
        title: 'List every supply',
        explanation:
          'The enhanced for loop visits supplies from first item to last. During each iteration, the loop variable supply holds the current String. The println instruction inside the braces therefore runs three times with three different values.',
        analogy:
          'A volunteer moves down the packing checklist one line at a time. A temporary finger points at Blankets, then Cups, then Napkins, while the same packing call is made for each line.',
        type: 'code',
        prompt: 'Replace the comment with an enhanced for loop that prints Supply: followed by each current supply.',
        starterCode: 'public class Main {\n    public static void main(String[] args) {\n        String[] supplies = { "Blankets", "Cups", "Napkins" };\n\n        // Visit and print every supply.\n    }\n}',
        focus: 'Write for (String supply : supplies) with one concatenated System.out.println inside its braces. There is no _____ blank.',
        codeGuide: [
          { code: 'for', plain: 'This keyword starts the enhanced loop that will visit each item in the supplied array exactly once.' },
          { code: 'String supply', plain: 'The current item is text, and supply is the temporary name used during one iteration.' },
          { code: ': supplies', plain: 'The colon can be read as in, while supplies names the ordered collection the loop should visit.' },
          { code: '{ ... }', plain: 'The loop braces group the instruction Java repeats once for every item in the array.' },
          { code: 'System.out.println("Supply: " + supply);', plain: 'The unquoted loop variable retrieves the current item, so each repeated output line names a different supply.' },
        ],
        checks: [
          { pattern: 'for\\s*\\(\\s*String\\s+supply\\s*:\\s*supplies\\s*\\)\\s*\\{[^}]*System\\.out\\.println\\s*\\(\\s*"Supply: "\\s*\\+\\s*supply\\s*\\)\\s*;[^}]*\\}', message: 'Use for (String supply : supplies) and print the current supply from inside its braces.' },
        ],
        output: 'Supply: Blankets\nSupply: Cups\nSupply: Napkins',
        hint: 'The loop header is for (String supply : supplies). Inside its braces, join supply to the fixed output label.',
        recap: 'An enhanced for loop visits every array item in order, and its loop variable holds one current value while the body repeats.',
        xp: 18,
      },
    },
    {
      id: 'project-java-method',
      order: 9,
      title: 'Package the picnic summary',
      objective: 'Define a reusable static void method with typed parameters, then call it with the stored organizer values as arguments.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'method', meaning: 'A named group of Java instructions that can be called whenever a program needs that job performed.' },
        { term: 'static', meaning: 'A Java modifier that lets main call this class method directly without first creating a Main object.' },
        { term: 'void', meaning: 'A return type stating that a method performs its job without returning a value to the caller.' },
        { term: 'parameter', meaning: 'A typed name in a method definition that receives one value supplied by a later call.' },
        { term: 'argument', meaning: 'The actual value or variable supplied to a parameter when the method is called.' },
        { term: 'method body', meaning: 'The brace-enclosed instructions that run each time a particular method is called.' },
      ],
      exercise: {
        id: 'project-java-method',
        conceptId: 'project-java-static-method-parameters',
        eyebrow: 'Checkpoint 9 of 12',
        title: 'Package the picnic summary',
        explanation:
          'printPicnic names a reusable job inside Main. static lets the static main method call it directly, and void says it performs output without returning a value. Its String name and int guests parameters receive the guestName and guestCount arguments from the later call.',
        analogy:
          'A label printer has two marked input slots: one for the organizer name and one for the guest count. Any picnic record can fill those slots, and the same installed routine prints the correct summary.',
        type: 'code',
        prompt: 'Replace the comment with the complete printPicnic method, then keep the supplied main method call that uses both variables.',
        starterCode: 'public class Main {\n    // Define printPicnic here.\n\n    public static void main(String[] args) {\n        String guestName = "Alex Kim";\n        int guestCount = 10;\n        printPicnic(guestName, guestCount);\n    }\n}',
        focus: 'Define static void printPicnic(String name, int guests) with the exact concatenated summary inside its braces. There is no _____ blank.',
        codeGuide: [
          { code: 'static void printPicnic', plain: 'static makes the method directly callable from main, void promises no returned value, and printPicnic names the job.' },
          { code: '(String name, int guests)', plain: 'These typed parameters create local names for the two values that each later method call supplies.' },
          { code: '{ ... }', plain: 'The method body contains the output instruction that runs every time printPicnic is called.' },
          { code: 'System.out.println("Picnic: " + name + " | Guests: " + guests);', plain: 'The method uses its parameter names to build a summary from whichever arguments it receives.' },
          { code: 'printPicnic(guestName, guestCount);', plain: 'This call supplies the stored organizer name to name and the stored count to guests in matching order.' },
        ],
        checks: [
          { pattern: 'static\\s+void\\s+printPicnic\\s*\\(\\s*String\\s+name\\s*,\\s*int\\s+guests\\s*\\)\\s*\\{[^}]*System\\.out\\.println\\s*\\(\\s*"Picnic: "\\s*\\+\\s*name\\s*\\+\\s*" \\| Guests: "\\s*\\+\\s*guests\\s*\\)\\s*;[^}]*\\}', message: 'Define static void printPicnic with String name and int guests, then print the exact summary inside.' },
          { pattern: 'printPicnic\\s*\\(\\s*guestName\\s*,\\s*guestCount\\s*\\)\\s*;', message: 'Keep the call that passes guestName first and guestCount second.' },
        ],
        output: 'Picnic: Alex Kim | Guests: 10',
        hint: 'Use the header static void printPicnic(String name, int guests), then place the summary println between its braces.',
        recap: 'A static method packages one reusable job. Typed parameters receive the arguments supplied in the later call from main.',
        xp: 20,
      },
    },
    {
      id: 'project-java-order',
      order: 10,
      title: 'Put the picnic workflow in order',
      objective: 'Retrieve the chosen source order that places every stored value before the picnic instructions that depend on it.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'dependency order', meaning: 'An arrangement where each instruction appears after the values or setup that it needs are available.' },
      ],
      exercise: {
        id: 'project-java-order',
        conceptId: 'project-java-program-order',
        eyebrow: 'Checkpoint 10 of 12',
        title: 'Put the picnic workflow in order',
        explanation:
          'A complete Java program is still a sequence of small jobs. The reusable method belongs directly in the class, not inside main. Inside main, the Scanner and supplies must exist before input and the loop use them. Both answers must be stored before the decision or final method call uses them. Java allows class methods in either member order, but this project keeps printPicnic above main so the finished source is easy to scan.',
        analogy:
          'The picnic team installs the summary label routine, opens the sign-in desk, prepares the supply list, records the organizer, chooses a table, packs every supply, and prints the completed plan.',
        type: 'ordering',
        prompt: 'Arrange the six larger jobs in the order used by the final Community Picnic Planner source.',
        orderItems: [
          { id: 'print-method', code: 'Define the reusable printPicnic class method' },
          { id: 'prepare-main', code: 'In main, create the Scanner and supplies array' },
          { id: 'read-answers', code: 'Ask for and store the organizer name and guest count' },
          { id: 'table-decision', code: 'Choose Large or Small with if and else' },
          { id: 'supply-loop', code: 'List each supply with an enhanced for loop' },
          { id: 'print-call', code: 'Call printPicnic with the stored organizer values' },
        ],
        correctOrder: [
          'print-method',
          'prepare-main',
          'read-answers',
          'table-decision',
          'supply-loop',
          'print-call',
        ],
        incorrectMessage: 'Keep the method at class level, prepare main, collect both answers, decide, loop through supplies, and call the summary last.',
        output: 'Method, setup, answers, table, supplies, summary',
        hint: 'Start with reusable class-level preparation, collect changing answers in the middle, and finish with output that needs those answers.',
        recap: 'Dependency order keeps setup and stored values above their uses: define, prepare, read, decide, repeat, and then print the final summary.',
        xp: 20,
      },
    },
    {
      id: 'project-java-assembly',
      order: 11,
      title: 'Connect the picnic helper',
      objective: 'Author the input, table decision, supply loop, and method call inside a supplied Java class and main program frame.',
      scaffolding: 'independent',
      newTerms: [],
      exercise: {
        id: 'project-java-assembly',
        conceptId: 'project-java-assembly',
        eyebrow: 'Checkpoint 11 of 12',
        title: 'Connect the picnic helper',
        explanation:
          'The import, class, reusable method, Scanner, and array are ready. Your instructions must ask for both answers, parse the guest count, choose exactly one table label, visit every supply, and finally pass the stored values into printPicnic.',
        analogy:
          'The picnic desk and packing list are already set up. You are opening the real workflow: listen to the organizer, select a table, pack each listed item, and send the finished record to the label printer.',
        type: 'code',
        prompt: 'Replace the four comments with complete input, decision, loop, and method-call instructions.',
        starterCode: 'import java.util.Scanner;\n\npublic class Main {\n    static void printPicnic(String name, int guests) {\n        System.out.println("Picnic: " + name + " | Guests: " + guests);\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        String[] supplies = { "Blankets", "Cups", "Napkins" };\n\n        // Ask for and store the organizer name and guest count.\n        // Print Large for at least eight guests, or Small otherwise.\n        // Print every supply with an enhanced for loop.\n        // Call printPicnic with the stored organizer values.\n    }\n}',
        focus: 'Write each missing section as complete Java code. There are no _____ blanks, and every later instruction should use the stored values prepared above it.',
        codeGuide: [
          { code: 'String value = scanner.nextLine();', plain: 'Use the familiar complete-line shape for the organizer name so internal spaces remain part of the stored text.' },
          { code: 'int number = Integer.parseInt(scanner.nextLine());', plain: 'Use the familiar nested-call shape to convert the next line of digit text into the guest-count integer.' },
          { code: 'if (number >= 8) { ... } else { ... }', plain: 'Place the two exact table outputs in separate branches so only one table size is printed.' },
          { code: 'for (String item : collection) { ... }', plain: 'Use the enhanced loop shape with the project names supply and supplies, then print the current value.' },
          { code: 'printPicnic(textValue, numberValue);', plain: 'Finish by passing the stored organizer name first and the stored guest count second.' },
        ],
        checks: [
          { pattern: 'String\\s+guestName\\s*=\\s*scanner\\.nextLine\\s*\\(\\s*\\)\\s*;', message: 'Read the complete organizer name into guestName with scanner.nextLine().' },
          { pattern: 'int\\s+guestCount\\s*=\\s*Integer\\.parseInt\\s*\\(\\s*scanner\\.nextLine\\s*\\(\\s*\\)\\s*\\)\\s*;', message: 'Parse the second input line into the guestCount integer.' },
          { pattern: 'if\\s*\\(\\s*guestCount\\s*>=\\s*8\\s*\\)[\\s\\S]*else', message: 'Choose a table with if (guestCount >= 8) and an else branch.' },
          { pattern: 'for\\s*\\(\\s*String\\s+supply\\s*:\\s*supplies\\s*\\)', message: 'Visit every stored supply with for (String supply : supplies).' },
          { pattern: 'printPicnic\\s*\\(\\s*guestName\\s*,\\s*guestCount\\s*\\)\\s*;', message: 'Finish by calling printPicnic with guestName and guestCount in that order.' },
        ],
        output: 'What is your name?\nHow many guests are coming?\nTable: Small\nSupply: Blankets\nSupply: Cups\nSupply: Napkins\nPicnic: Jamie Chen | Guests: 6',
        hint: 'Follow checkpoint 10: read both values, decide the table, loop through supplies, and call printPicnic last.',
        recap: 'You connected prepared pieces through their real dependencies. Scanner input feeds the decision and method call, while the enhanced for loop visits the array.',
        xp: 26,
      },
      practiceStdin: 'Jamie Chen\n6\n',
    },
    {
      id: 'project-java-final',
      order: 12,
      title: 'Plan the Community Picnic',
      objective: 'Build a complete Main.java program and prove that changing organizer input changes its table choice and final summary.',
      scaffolding: 'independent',
      newTerms: [
        { term: 'test case', meaning: 'One set of program input and expected behavior used to check whether a solution works.' },
        { term: 'hidden check', meaning: 'An official test whose exact values stay private so the program must solve the general task.' },
      ],
      requirements: [
        'Begin with import java.util.Scanner; so the short Scanner class name is available.',
        'Use one public class named Main so the downloaded Main.java file and public class agree.',
        'Define static void printPicnic(String name, int guests) as a class method outside main.',
        'Have printPicnic display Picnic: followed by name, a vertical separator, and Guests: followed by guests.',
        'Use public static void main(String[] args) as the program entry point.',
        'Inside main, create Scanner scanner = new Scanner(System.in); exactly once.',
        'Create the supplies String array with Blankets, Cups, and Napkins in that order.',
        'Ask What is your name? and store scanner.nextLine() in guestName, then ask How many guests are coming? and parse the next line into guestCount.',
        'Print Table: Large when guestCount is at least 8, or Table: Small otherwise.',
        'Use for (String supply : supplies) to print Supply: followed by every supply.',
        'Call printPicnic(guestName, guestCount) after the supply loop.',
      ],
      exercise: {
        id: 'project-java-final',
        conceptId: 'project-java-final-picnic-planner',
        eyebrow: 'Checkpoint 12 of 12',
        title: 'Plan the Community Picnic',
        explanation:
          'This final Main.java source combines an import, a class and main frame, one Scanner, typed variables, integer parsing, an array, a two-route decision, an enhanced for loop, concatenation, and one static method with two parameters. Each organizer answer travels through several visible program decisions.',
        analogy:
          'You are opening the complete picnic desk. The sign-in form, table rule, packing checklist, and summary printer now work as one dependable process for every organizer who arrives.',
        type: 'code',
        prompt: 'Complete the Community Picnic Planner from the written requirements. The visible example uses Alex Kim and 10, while official checks use other values.',
        starterCode: 'import java.util.Scanner;\n\npublic class Main {\n    // Define printPicnic with its two typed parameters.\n\n    public static void main(String[] args) {\n        // Create the Scanner and three-item supplies array.\n\n        // Ask for and store the organizer name.\n        // Ask for and parse the guest count.\n\n        // Choose and print the Large or Small table.\n\n        // Visit and print every supply.\n\n        // Print the final picnic summary with the stored values.\n    }\n}',
        focus: 'Write every missing Java statement as complete source. There are no _____ blanks, and the official organizer name and guest count will change.',
        codeGuide: [
          { code: 'import java.util.Scanner;', plain: 'Keep this supplied import before Main so the source can use the short Scanner class name.' },
          { code: 'public class Main { ... }', plain: 'Keep the required class and place printPicnic and main directly inside its outer braces.' },
          { code: 'static void method(String text, int number) { ... }', plain: 'Use this familiar method shape with the required printPicnic, name, and guests identifiers.' },
          { code: 'Scanner value = new Scanner(System.in);', plain: 'Create the one console reader before either input instruction needs to use it.' },
          { code: 'String[] collection = { "First", "Second" };', plain: 'Use the project array name and exact three supply Strings in their required written order.' },
          { code: 'scanner.nextLine() and Integer.parseInt(...)', plain: 'Read both answers as complete lines, then wrap the numeric answer in parseInt before storing it.' },
          { code: 'if (...) { ... } else { ... }', plain: 'Compare the stored guest count with eight and print exactly one table-size line.' },
          { code: 'for (String item : collection) { ... }', plain: 'Visit the stored supplies in order and join the current supply into one repeated output line.' },
          { code: 'Run before Check', plain: 'Experiment with the visible input first, then use the official check for server-owned behavior and source requirements.' },
        ],
        checks: [
          { pattern: '^\\s*import\\s+java\\.util\\.Scanner\\s*;', message: 'Begin with import java.util.Scanner; so the source can use Scanner.' },
          { pattern: 'public\\s+class\\s+Main\\s*\\{', message: 'Keep one public class named Main around the two required methods.' },
          { pattern: 'public\\s+static\\s+void\\s+main\\s*\\(\\s*String\\s*\\[\\s*\\]\\s+args\\s*\\)', message: 'Use public static void main(String[] args) as the exact entry point.' },
          { pattern: 'static\\s+void\\s+printPicnic\\s*\\(\\s*String\\s+name\\s*,\\s*int\\s+guests\\s*\\)', message: 'Define printPicnic with the exact String name and int guests parameters.' },
          { pattern: 'System\\.out\\.println\\s*\\(\\s*"Picnic: "\\s*\\+\\s*name\\s*\\+\\s*" \\| Guests: "\\s*\\+\\s*guests\\s*\\)\\s*;', message: 'Print the exact concatenated picnic summary from inside printPicnic.' },
          { pattern: 'Scanner\\s+scanner\\s*=\\s*new\\s+Scanner\\s*\\(\\s*System\\.in\\s*\\)\\s*;', message: 'Create one Scanner named scanner from System.in inside main.' },
          { pattern: 'String\\s*\\[\\s*\\]\\s+supplies\\s*=\\s*\\{\\s*"Blankets"\\s*,\\s*"Cups"\\s*,\\s*"Napkins"\\s*\\}\\s*;', message: 'Create the supplies array with Blankets, Cups, and Napkins in that order.' },
          { pattern: 'String\\s+guestName\\s*=\\s*scanner\\.nextLine\\s*\\(\\s*\\)\\s*;', message: 'Read the complete organizer name into guestName with scanner.nextLine().' },
          { pattern: 'int\\s+guestCount\\s*=\\s*Integer\\.parseInt\\s*\\(\\s*scanner\\.nextLine\\s*\\(\\s*\\)\\s*\\)\\s*;', message: 'Parse the guest count from the second input line with Integer.parseInt.' },
          { pattern: 'if\\s*\\(\\s*guestCount\\s*>=\\s*8\\s*\\)[\\s\\S]*System\\.out\\.println\\s*\\(\\s*"Table: Large"\\s*\\)[\\s\\S]*else[\\s\\S]*System\\.out\\.println\\s*\\(\\s*"Table: Small"\\s*\\)', message: 'Use guestCount >= 8 with the exact Large and Small output lines.' },
          { pattern: 'for\\s*\\(\\s*String\\s+supply\\s*:\\s*supplies\\s*\\)[\\s\\S]*System\\.out\\.println\\s*\\(\\s*"Supply: "\\s*\\+\\s*supply\\s*\\)', message: 'Loop through supplies with the supply variable and print each current value.' },
          { pattern: 'printPicnic\\s*\\(\\s*guestName\\s*,\\s*guestCount\\s*\\)\\s*;', message: 'Call printPicnic with guestName first and guestCount second after the loop.' },
        ],
        output: finalProjectVisibleTestCase.expectedStdout,
        hint: 'Build from checkpoint 11. Keep the class-level method first, then prepare, read, decide, repeat, and call printPicnic last.',
        recap: 'You built a complete Java picnic planner that reads, parses, decides, repeats, and calls a reusable static method with changing organizer data.',
        xp: 40,
      },
      practiceStdin: finalProjectVisibleTestCase.stdin,
      assessmentSummary: {
        visibleTestCase: finalProjectVisibleTestCase,
        hiddenTestCount: 3,
        structuralCheckCount: 9,
      },
    },
  ],
} satisfies JavaPicnicProject
