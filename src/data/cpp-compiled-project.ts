import { cppCompiledProjectManifest } from './cpp-compiled-project-manifest'
import type { GuidedProject, GuidedProjectCheckpoint, ProjectTestCase } from './project-types'

export type CppProjectCheckpoint = GuidedProjectCheckpoint
export type CppCompiledProject = GuidedProject

const finalProjectVisibleTestCase: ProjectTestCase = {
  id: 'final-visible-two-details',
  name: 'A two-detail observation',
  visibility: 'visible',
  stdin: 'Avery\n2\n',
  expectedStdout: [
    'Welcome to the Observation Desk!',
    'What is your name?',
    'How many details did you notice?',
    'Avery, you recorded 2 details and earned 10 focus points.',
  ].join('\n'),
  purpose: 'Shows one complete observation before the official check uses different names and counts.',
}

export const cppCompiledProject: CppCompiledProject = {
  ...cppCompiledProjectManifest,
  checkpoints: [
    {
      id: 'project-cpp-compiler-path',
      order: 1,
      title: 'Turn source into a program',
      objective: 'Recognize the compiler step between writing C++ source code and running a finished program.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'source code', meaning: 'The human-readable instructions that a programmer writes in a file.' },
        { term: 'compiler', meaning: 'A tool that checks and translates C++ source before the program can run.' },
        { term: 'executable', meaning: 'The built program that the computer can start after compilation succeeds.' },
        { term: 'compile', meaning: 'To ask the compiler to check and translate source code into a program.' },
        { term: 'run', meaning: 'To start a compiled program so the computer follows its instructions.' },
      ],
      exercise: {
        id: 'project-cpp-compiler-path',
        conceptId: 'project-cpp-compile-run-cycle',
        eyebrow: 'Checkpoint 1 of 12',
        title: 'Turn source into a program',
        explanation:
          'C++ source code does not run directly. A compiler first reads the source, reports structural problems it can find, and translates successful code into an executable program that the computer can start.',
        analogy:
          'A written recipe is useful to a cook, but it is not yet a finished meal. Source is the recipe, compilation is the preparation, and running is the moment the finished program is served.',
        type: 'choice',
        prompt: 'What happens after you write C++ source code but before the finished program runs?',
        choices: [
          { id: 'a', label: 'A compiler checks and translates the source', detail: 'Successful compilation produces the program that the computer can start.' },
          { id: 'b', label: 'Saving automatically runs every instruction', detail: 'Saving keeps the source file, but it does not replace compilation or start the program.' },
          { id: 'c', label: 'The console invents missing instructions', detail: 'The console can show output, but it does not write or translate the program.' },
        ],
        correctChoice: 'a',
        hint: 'Look for the tool that stands between human-readable source and the executable program.',
        recap: 'Source code is the human-readable recipe. The compiler checks and translates it, and running starts the resulting executable.',
        xp: 8,
      },
    },
    {
      id: 'project-cpp-program-frame',
      order: 2,
      title: 'Find the starting doorway',
      objective: 'Identify main as the function where a finished C++ program begins its own instructions.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'function', meaning: 'A named group of instructions that performs one part of a program.' },
        { term: 'entry point', meaning: 'The function where the operating system starts a program. In this project, that function is main.' },
        { term: 'brace', meaning: 'One of the matching { and } symbols that marks the beginning and end of a code group.' },
        { term: 'return value', meaning: 'A value a function gives back when it finishes. Here, 0 reports a normal finish.' },
      ],
      exercise: {
        id: 'project-cpp-program-frame',
        conceptId: 'project-cpp-program-frame',
        eyebrow: 'Checkpoint 2 of 12',
        title: 'Find the starting doorway',
        explanation:
          'A small C++ program begins its work in a function named main. The opening brace starts the function body, the closing brace ends it, and return 0 reports that the program reached a normal finish.',
        analogy:
          'A public building may contain many rooms, but visitors still need one marked entrance. main is the entrance the computer uses before it can reach the instructions inside the braces.',
        type: 'prediction',
        prompt: 'When this compiled program starts, which named function is its starting doorway?',
        displayCode: '#include <iostream>\n\nint main() {\n    std::cout << "Observation desk ready.\\n";\n    return 0;\n}',
        choices: [
          { id: 'a', label: 'iostream', detail: 'iostream is the supplied console header, not the program entry point.' },
          { id: 'b', label: 'main', detail: 'The operating system begins this program by calling the main function.' },
          { id: 'c', label: 'return', detail: 'return finishes the function; it does not name the starting doorway.' },
        ],
        correctChoice: 'b',
        hint: 'Find the name immediately before the parentheses and the opening function brace.',
        recap: 'main is the starting doorway for this C++ program. Its braces contain the instructions that run, and return 0 reports a normal finish.',
        xp: 8,
      },
    },
    {
      id: 'project-cpp-output',
      order: 3,
      title: 'Send one clear line',
      objective: 'Use the standard C++ output stream to display one complete line in the console.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'header', meaning: 'A supplied file that declares tools a C++ program wants to use, such as console input and output.' },
        { term: 'standard library', meaning: 'A collection of ready-made C++ tools whose names commonly begin with std::.' },
        { term: 'output stream', meaning: 'A destination that receives values in order. std::cout sends those values to the console.' },
        { term: 'insertion operator', meaning: 'The << symbols that route the value on their right into the stream on their left.' },
        { term: 'newline escape sequence', meaning: 'The two source characters \\n inside a string that move console output to the next line.' },
      ],
      exercise: {
        id: 'project-cpp-output',
        conceptId: 'project-cpp-console-output',
        eyebrow: 'Checkpoint 3 of 12',
        title: 'Send one clear line',
        explanation:
          'The iostream header supplies standard console streams. std::cout is the output stream, << inserts the quoted string into it, and \\n moves the console to a fresh line after the message.',
        analogy:
          'Think of std::cout as a labeled delivery chute. The << symbols point a package into the chute, while \\n tells the next package to begin on a clean shelf.',
        type: 'code',
        prompt: 'Replace the blank with the C++ output stream that displays Observation desk ready.',
        starterCode: '#include <iostream>\n\nint main() {\n    _____ << "Observation desk ready.\\n";\n    return 0;\n}',
        focus: 'Replace only the _____ blank with std::cout. Keep the arrows, quoted message, newline, and semicolon in place.',
        codeGuide: [
          { code: '#include <iostream>', plain: 'This supplied line makes the standard console input and output declarations available to the source file.' },
          { code: 'int main() {', plain: 'This supplied function is the starting doorway, and its opening brace begins the instructions that will run.' },
          { code: 'std::cout', plain: 'This standard-library output stream sends the values inserted into it to the text console.' },
          { code: '<< "...\\n";', plain: 'The arrows route the string to the stream, \\n starts a new output line, and the semicolon ends the instruction.' },
          { code: 'return 0;', plain: 'This supplied final instruction reports a normal finish after the output instruction has run.' },
        ],
        checks: [
          { pattern: 'std::cout\\s*<<\\s*"Observation desk ready\\.\\\\n"\\s*;', message: 'Use std::cout before the arrows and keep the exact message, newline, and semicolon.' },
        ],
        output: 'Observation desk ready.',
        hint: 'The completed instruction begins std::cout << and keeps the supplied message after the arrows.',
        recap: 'std::cout is a standard output stream. The << operator inserts a value, and \\n moves later console output to the next line.',
        xp: 12,
      },
    },
    {
      id: 'project-cpp-semicolon',
      order: 4,
      title: 'Read your first compiler message',
      objective: 'Use a compiler diagnostic to find and repair one missing C++ statement ending.',
      scaffolding: 'guided',
      newTerms: [
        { term: 'statement', meaning: 'One complete C++ instruction, usually finished with a semicolon.' },
        { term: 'semicolon', meaning: 'The ; punctuation mark that tells the compiler where most C++ statements end.' },
        { term: 'compile error', meaning: 'A problem that prevents the compiler from building a runnable program.' },
        { term: 'diagnostic', meaning: 'A compiler message that describes a problem and points toward the source location where it was noticed.' },
        { term: 'line number', meaning: 'The numbered source line a tool identifies so a programmer knows where to begin inspecting code.' },
      ],
      exercise: {
        id: 'project-cpp-semicolon',
        conceptId: 'project-cpp-compile-diagnostic',
        eyebrow: 'Checkpoint 4 of 12',
        title: 'Read your first compiler message',
        explanation:
          'The output statement is missing its final semicolon, so the compiler cannot see where that instruction ends. It reports a compile error before the program gets a chance to run.',
        analogy:
          'A sentence without its final punctuation can run into the sentence after it. The compiler stops at the confusing boundary and gives you a location to inspect.',
        type: 'bugfix',
        prompt: 'Run the broken program, read the semicolon diagnostic, then add the one missing statement ending.',
        starterCode: '#include <iostream>\n\nint main() {\n    std::cout << "Details recorded.\\n"\n    return 0;\n}',
        focus: 'First use Run to see the compiler message. Then add one semicolon after the closing quotation mark on the output line.',
        codeGuide: [
          { code: 'Run before editing', plain: 'The starter is intentionally broken so the real compiler can show what it notices before you repair the source.' },
          { code: 'expected ;', plain: 'This diagnostic wording means the compiler reached a place where it needed a statement-ending semicolon.' },
          { code: 'line 4', plain: 'A reported line is the first place to inspect, but the missing punctuation may sit at the end of that line or just above it.' },
          { code: '"Details recorded.\\n";', plain: 'The semicolon belongs after the complete quoted string and before the next statement begins.' },
          { code: 'return 0;', plain: 'Keep this supplied statement unchanged. The problem is the unfinished output statement immediately before it.' },
        ],
        checks: [
          { pattern: 'std::cout\\s*<<\\s*"Details recorded\\.\\\\n"\\s*;', message: 'Add one semicolon after the output string so the statement has a clear ending.' },
          { pattern: 'return\\s+0\\s*;', message: 'Keep return 0; so main still reports a normal finish after the repair.' },
        ],
        output: 'Details recorded.',
        hint: 'Place ; immediately after the closing quotation mark on the std::cout line.',
        recap: 'A compile error happens before a program runs. Read the friendly diagnostic, inspect its line, and repair the smallest identified problem first.',
        xp: 14,
      },
      expectedFirstRun: {
        outcome: 'compile_error',
        diagnosticTitle: 'C++ expected a semicolon',
        explanation: 'The output statement has no semicolon, so the compiler cannot finish building the program yet.',
      },
    },
    {
      id: 'project-cpp-string',
      order: 5,
      title: "Store the observer's name",
      objective: 'Initialize a declared C++ text variable and retrieve its stored value for console output.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'type', meaning: 'A category that tells C++ what kind of value a variable is allowed to store and use.' },
        { term: 'declaration', meaning: 'A statement that introduces a variable by giving C++ its type and its name.' },
        { term: 'initialization', meaning: 'Giving a variable its first stored value as part of the declaration.' },
        { term: 'std::string', meaning: 'The standard-library type used here to store a sequence of text characters.' },
      ],
      exercise: {
        id: 'project-cpp-string',
        conceptId: 'project-cpp-string-declaration',
        eyebrow: 'Checkpoint 5 of 12',
        title: "Store the observer's name",
        explanation:
          'C++ variable declarations state a type before a name. std::string permits text, observer_name labels the storage, and the value after = initializes that variable before later code retrieves it.',
        analogy:
          'A labeled drawer works best when its label also says what belongs inside. std::string marks this drawer for text, observer_name names it, and Avery is the first stored card.',
        type: 'code',
        prompt: 'Initialize observer_name with the quoted text Avery so the existing output statement can retrieve it.',
        starterCode: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string observer_name = _____;\n    std::cout << observer_name << "\\n";\n    return 0;\n}',
        focus: 'Replace only the _____ blank with the quoted string "Avery". Keep the type, variable name, equals sign, and semicolon.',
        codeGuide: [
          { code: '#include <string>', plain: 'This supplied header makes the standard std::string text type available to the source file.' },
          { code: 'std::string', plain: 'This type tells C++ that observer_name will store a sequence of text characters.' },
          { code: 'observer_name', plain: 'This descriptive variable name labels the value so later statements can retrieve it without quotation marks.' },
          { code: '= "Avery";', plain: 'The equals sign supplies the first value, quotation marks identify text, and the semicolon finishes the declaration.' },
          { code: 'std::cout << observer_name', plain: 'Writing the variable name without quotes retrieves its stored value and inserts that value into the output stream.' },
        ],
        checks: [
          { pattern: 'std::string\\s+observer_name\\s*=\\s*"Avery"\\s*;', message: 'Initialize observer_name with the exact quoted string "Avery".' },
          { pattern: 'std::cout\\s*<<\\s*observer_name', message: 'Keep the output statement that retrieves observer_name without quotation marks.' },
        ],
        output: 'Avery',
        hint: 'The complete declaration is std::string observer_name = "Avery";',
        recap: 'A C++ declaration names both a type and a variable. Initialization gives that variable its first value in the same statement.',
        xp: 14,
      },
    },
    {
      id: 'project-cpp-integer',
      order: 6,
      title: 'Store a whole-number rule',
      objective: 'Initialize an int variable with the whole-number value used by the later score calculation.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'integer', meaning: 'A whole number without a decimal part, such as 0, 5, or 42.' },
        { term: 'int', meaning: 'The built-in C++ type used here to store a whole-number value.' },
      ],
      exercise: {
        id: 'project-cpp-integer',
        conceptId: 'project-cpp-integer-declaration',
        eyebrow: 'Checkpoint 6 of 12',
        title: 'Store a whole-number rule',
        explanation:
          'The int type stores whole numbers. points_per_detail will hold the rule that every observed detail earns five points, so the value 5 is written without quotation marks.',
        analogy:
          'A scoring card can print the rule five points per detail. Keeping 5 as an integer lets the program use that rule in arithmetic instead of treating it as printed text.',
        type: 'code',
        prompt: 'Initialize points_per_detail with the whole number 5, then let the existing statement display it.',
        starterCode: '#include <iostream>\n\nint main() {\n    int points_per_detail = _____;\n    std::cout << points_per_detail << "\\n";\n    return 0;\n}',
        focus: 'Replace only the _____ blank with 5. Do not add quotation marks around the whole number.',
        codeGuide: [
          { code: 'int', plain: 'This built-in type reserves storage for a whole number that the program can use in arithmetic.' },
          { code: 'points_per_detail', plain: 'This name explains that the stored number is a rule applied once for every observed detail.' },
          { code: '= 5;', plain: 'This initializes the integer with five, and the semicolon finishes the complete declaration statement.' },
          { code: '5 versus "5"', plain: 'The unquoted 5 is an integer. Quoted digits form text and are not the value requested by this declaration.' },
          { code: 'std::cout << points_per_detail', plain: 'The output statement retrieves the integer and sends its visible digits to the console.' },
        ],
        checks: [
          { pattern: 'int\\s+points_per_detail\\s*=\\s*5\\s*;', message: 'Initialize points_per_detail with the unquoted integer 5.' },
          { pattern: 'std::cout\\s*<<\\s*points_per_detail', message: 'Keep the output statement that retrieves points_per_detail.' },
        ],
        output: '5',
        hint: 'The repaired declaration is int points_per_detail = 5; with no quotation marks.',
        recap: 'int stores whole numbers. Leaving quotation marks off 5 lets C++ use that stored value in a later numeric calculation.',
        xp: 14,
      },
    },
    {
      id: 'project-cpp-arithmetic',
      order: 7,
      title: 'Calculate focus points',
      objective: 'Multiply two stored C++ integers and initialize a third variable with the calculated result.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'operator', meaning: 'A symbol that tells C++ to perform an operation. The * operator means multiplication here.' },
        { term: 'expression', meaning: 'A piece of code that C++ evaluates to produce a value, such as the result of multiplication.' },
      ],
      exercise: {
        id: 'project-cpp-arithmetic',
        conceptId: 'project-cpp-arithmetic',
        eyebrow: 'Checkpoint 7 of 12',
        title: 'Calculate focus points',
        explanation:
          'The * operator multiplies integer values. C++ evaluates details * points_per_detail first, then uses the resulting value to initialize the focus_points variable.',
        analogy:
          'A judge counts three details and awards five points for each one. Multiplication performs the scorecard work, while focus_points labels the calculated total for later use.',
        type: 'code',
        prompt: 'Complete the expression that calculates focus_points from details and points_per_detail.',
        starterCode: '#include <iostream>\n\nint main() {\n    int details = 3;\n    int points_per_detail = 5;\n    int focus_points = _____ * _____;\n\n    std::cout << focus_points << "\\n";\n    return 0;\n}',
        focus: 'Replace the first blank with details and the second with points_per_detail. Keep the * operator between the two names.',
        codeGuide: [
          { code: 'details = 3', plain: 'This prepared integer records how many details belong to the sample observation.' },
          { code: 'points_per_detail = 5', plain: 'This prepared integer holds the scoring rule applied to every recorded detail.' },
          { code: 'details * points_per_detail', plain: 'The * operator retrieves and multiplies the two integer values, producing fifteen.' },
          { code: 'int focus_points = ...;', plain: 'The declaration initializes a new integer with the result produced by the expression.' },
          { code: 'std::cout << focus_points', plain: 'The final statement retrieves the calculated result and sends it to the console.' },
        ],
        checks: [
          { pattern: 'int\\s+focus_points\\s*=\\s*details\\s*\\*\\s*points_per_detail\\s*;', message: 'Initialize focus_points with details * points_per_detail.' },
          { pattern: 'std::cout\\s*<<\\s*focus_points', message: 'Keep the output statement that displays the calculated focus_points value.' },
        ],
        output: '15',
        hint: 'The complete calculation is int focus_points = details * points_per_detail;',
        recap: 'An expression produces a value. A typed declaration can initialize a new variable with that calculated result for later use.',
        xp: 16,
      },
    },
    {
      id: 'project-cpp-line-input',
      order: 8,
      title: 'Read a full name',
      objective: 'Use std::getline with the standard input stream to store a complete typed name, including spaces.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'input stream', meaning: 'A source of incoming values. std::cin receives the text a person types into the console.' },
        { term: 'std::cin', meaning: 'The standard input stream that receives characters typed for a console program.' },
        { term: 'std::getline', meaning: 'A standard function that reads one complete input line into a string variable.' },
        { term: 'argument', meaning: 'A value supplied inside a function call so the function knows what information to use.' },
      ],
      exercise: {
        id: 'project-cpp-line-input',
        conceptId: 'project-cpp-getline-input',
        eyebrow: 'Checkpoint 8 of 12',
        title: 'Read a full name',
        explanation:
          'std::getline reads characters from the std::cin input stream until the person presses Enter. It stores the complete line in observer_name, so a name containing a space stays together.',
        analogy:
          'A receptionist listens until a visitor finishes saying the whole name. getline behaves the same way: it keeps the full line rather than stopping at the first space.',
        type: 'code',
        prompt: 'Replace the blank with std::getline so the program reads and remembers the complete observer name.',
        starterCode: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string observer_name;\n\n    std::cout << "What is your name?\\n";\n    _____(std::cin, observer_name);\n\n    std::cout << "Hello, " << observer_name << "!\\n";\n    return 0;\n}',
        focus: 'Replace only the _____ blank with std::getline. Keep std::cin and observer_name as its two supplied arguments.',
        codeGuide: [
          { code: 'std::string observer_name;', plain: 'This declaration creates text storage without choosing a fixed name value ahead of time.' },
          { code: 'std::cout << "What is your name?\\n";', plain: 'The prompt tells the person what to enter before the program waits for incoming text.' },
          { code: 'std::getline(...)', plain: 'This function reads one complete line, including internal spaces, and stops when the person presses Enter.' },
          { code: 'std::cin', plain: 'The first argument identifies the standard console input stream as the place to read characters from.' },
          { code: 'observer_name', plain: 'The second argument identifies the string variable where getline should store the complete line.' },
        ],
        checks: [
          { pattern: 'std::getline\\s*\\(\\s*std::cin\\s*,\\s*observer_name\\s*\\)\\s*;', message: 'Call std::getline with std::cin first and observer_name second.' },
          { pattern: 'std::cout\\s*<<\\s*"Hello, "\\s*<<\\s*observer_name', message: 'Keep the greeting that retrieves and displays the stored full name.' },
        ],
        output: 'What is your name?\nHello, Alex Kim!',
        hint: 'The completed input statement is std::getline(std::cin, observer_name);',
        recap: 'std::getline reads a complete line from std::cin into a string. It preserves spaces inside a name such as Alex Kim.',
        xp: 18,
      },
      practiceStdin: 'Alex Kim\n',
    },
    {
      id: 'project-cpp-number-input',
      order: 9,
      title: 'Read a number',
      objective: 'Write a complete extraction statement that reads typed whole-number input into an int variable.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'extraction operator', meaning: 'The >> symbols that read the next suitable value from an input stream into a variable.' },
        { term: 'typed input', meaning: 'Input read directly into storage whose declared type tells C++ what kind of value to expect.' },
      ],
      exercise: {
        id: 'project-cpp-number-input',
        conceptId: 'project-cpp-stream-extraction',
        eyebrow: 'Checkpoint 9 of 12',
        title: 'Read a number',
        explanation:
          'The declaration makes details an int. std::cin >> details then asks the input stream for a whole number and stores the successful numeric value directly in that typed variable.',
        analogy:
          'A form with a whole-number box expects a count rather than a sentence. The >> operator carries the next suitable typed answer from the input line into that box.',
        type: 'code',
        prompt: 'Replace the comment with a complete std::cin statement that reads one whole number into details.',
        starterCode: '#include <iostream>\n\nint main() {\n    int details = 0;\n\n    std::cout << "How many details did you notice?\\n";\n    // Read one whole number into details.\n\n    std::cout << "You recorded " << details << " details.\\n";\n    return 0;\n}',
        focus: 'Delete the input comment and write the complete statement std::cin >> details; in its place. There is no _____ blank.',
        codeGuide: [
          { code: 'int details = 0;', plain: 'This declaration prepares whole-number storage and gives it a safe starting value before input arrives.' },
          { code: 'std::cin', plain: 'This is the same standard input stream used by getline, now reading a value by its declared type.' },
          { code: '>>', plain: 'The extraction arrows point from the input stream on the left toward the destination variable on the right.' },
          { code: 'details', plain: 'Because this destination was declared as int, C++ attempts to read a whole number into it.' },
          { code: 'std::cin >> details;', plain: 'The stream, extraction operator, destination, and semicolon form one complete input statement.' },
        ],
        checks: [
          { pattern: 'std::cin\\s*>>\\s*details\\s*;', message: 'Write std::cin >> details; as a complete statement.' },
          { pattern: 'int\\s+details\\s*=\\s*0\\s*;', message: 'Keep details declared as an int with its safe starting value.' },
        ],
        output: 'How many details did you notice?\nYou recorded 4 details.',
        hint: 'The extraction statement is std::cin >> details; with the arrows pointing toward the destination variable.',
        recap: 'std::cin >> details reads a whole number directly into the int variable. The declared type tells C++ what kind of input to attempt.',
        xp: 18,
      },
      practiceStdin: '4\n',
    },
    {
      id: 'project-cpp-report',
      order: 10,
      title: 'Build the observation report',
      objective: 'Write one complete output chain that combines fixed text with three stored C++ values.',
      scaffolding: 'supported',
      newTerms: [
        { term: 'output chain', meaning: 'One std::cout statement that uses several << operators to send multiple values in order.' },
      ],
      exercise: {
        id: 'project-cpp-report',
        conceptId: 'project-cpp-output-chain',
        eyebrow: 'Checkpoint 10 of 12',
        title: 'Build the observation report',
        explanation:
          'One std::cout statement can chain strings and variables from left to right. Each << inserts the next value, so the fixed words and live values become one readable console line.',
        analogy:
          'A report form alternates printed labels with filled-in facts. The output chain follows the same order, adding each fixed phrase and stored value to one final line.',
        type: 'code',
        prompt: 'Replace the comment with one std::cout chain that prints the complete personal observation report.',
        starterCode: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string observer_name = "Avery";\n    int details = 2;\n    int focus_points = 10;\n\n    // Display the complete personal report here.\n\n    return 0;\n}',
        focus: 'Write one complete std::cout statement that uses observer_name, details, and focus_points in that order. There is no _____ blank.',
        codeGuide: [
          { code: 'std::cout << observer_name', plain: 'Begin the output chain by inserting the live observer name instead of writing a fixed sample name.' },
          { code: '<< ", you recorded " << details', plain: 'Insert fixed connecting words, then insert the live integer count stored in details.' },
          { code: '<< " details and earned "', plain: 'This fixed phrase explains what the next calculated value represents in the report.' },
          { code: '<< focus_points', plain: 'Insert the calculated integer so the report changes whenever the earlier input changes.' },
          { code: '<< " focus points.\\n";', plain: 'Finish with fixed text, a newline, and the semicolon that ends the complete output statement.' },
        ],
        checks: [
          { pattern: 'std::cout\\s*<<[^;]*observer_name[^;]*details[^;]*focus_points[^;]*;', message: 'Use one std::cout chain containing observer_name, details, and focus_points in that order.' },
        ],
        output: 'Avery, you recorded 2 details and earned 10 focus points.',
        hint: 'Start with std::cout << observer_name, then keep adding the fixed phrases and variables with more << operators.',
        recap: 'An output chain inserts each fixed phrase and stored value from left to right. Live variables make the report respond to changing data.',
        xp: 20,
      },
    },
    {
      id: 'project-cpp-assembly',
      order: 11,
      title: 'Connect the pieces',
      objective: 'Author the complete input, calculation, and report statements inside a supplied C++ program frame.',
      scaffolding: 'independent',
      newTerms: [],
      exercise: {
        id: 'project-cpp-assembly',
        conceptId: 'project-cpp-assembly',
        eyebrow: 'Checkpoint 11 of 12',
        title: 'Connect the pieces',
        explanation:
          'A working program connects small instructions in dependency order. It declares storage before reading into it, collects both answers before calculating, and calculates before reporting the result.',
        analogy:
          'An observation card must have labeled spaces before facts can be written down. The facts must arrive before the score is calculated, and the score must exist before the report is filed.',
        type: 'code',
        prompt: 'Replace the four comments with complete declarations, input statements, the calculation, and the personal report.',
        starterCode: '#include <iostream>\n#include <string>\n\nint main() {\n    int points_per_detail = 5;\n\n    std::cout << "What is your name?\\n";\n    // Declare observer_name and read the full line.\n\n    std::cout << "How many details did you notice?\\n";\n    // Declare details and read the whole number.\n\n    // Calculate focus_points.\n    // Print the personal observation report.\n\n    return 0;\n}',
        focus: 'Write every missing instruction as complete C++ code. There are no _____ blanks, and each later step must use the values prepared above it.',
        codeGuide: [
          { code: 'std::string name; std::getline(std::cin, name);', plain: 'Use this familiar two-statement shape to declare text storage and then read one complete input line.' },
          { code: 'int count = 0; std::cin >> count;', plain: 'Use this familiar two-statement shape to prepare whole-number storage and extract a typed value into it.' },
          { code: 'int result = amount * rule;', plain: 'Use the project variable names in this general multiplication shape so the calculated value changes with input.' },
          { code: 'std::cout << value << " words";', plain: 'Build the report by alternating stored values and fixed strings with repeated << operators.' },
          { code: 'top to bottom', plain: 'Keep declaration before input, input before calculation, and calculation before the report that retrieves the result.' },
        ],
        checks: [
          { pattern: 'std::string\\s+observer_name\\s*;', message: 'Declare observer_name as a std::string before reading the full name.' },
          { pattern: 'std::getline\\s*\\(\\s*std::cin\\s*,\\s*observer_name\\s*\\)\\s*;', message: 'Read the full input line into observer_name with std::getline.' },
          { pattern: 'int\\s+details\\s*=\\s*0\\s*;', message: 'Declare details as an int with a safe starting value.' },
          { pattern: 'std::cin\\s*>>\\s*details\\s*;', message: 'Read the whole-number input into details.' },
          { pattern: 'int\\s+focus_points\\s*=\\s*details\\s*\\*\\s*points_per_detail\\s*;', message: 'Calculate focus_points from details * points_per_detail.' },
          { pattern: 'std::cout\\s*<<[^;]*observer_name[^;]*details[^;]*focus_points[^;]*;', message: 'Build one report chain containing observer_name, details, and focus_points.' },
        ],
        output: 'What is your name?\nHow many details did you notice?\nJordan Lee, you recorded 3 details and earned 15 focus points.',
        hint: 'Reuse the six familiar statement shapes from checkpoints 7 through 10, in the same top-to-bottom order.',
        recap: 'Larger programs are connected small steps. Declare, read, calculate, and report in an order where every instruction has the value it needs.',
        xp: 24,
      },
      practiceStdin: 'Jordan Lee\n3\n',
    },
    {
      id: 'project-cpp-final',
      order: 12,
      title: 'Build the Observation Desk',
      objective: 'Build a complete compiled C++ program and prove that changing input changes its calculated report.',
      scaffolding: 'independent',
      newTerms: [
        { term: 'test case', meaning: 'One set of program input and expected behavior used to check whether a solution works.' },
        { term: 'hidden check', meaning: 'An official test whose exact values are withheld so the program must solve the general task.' },
      ],
      requirements: [
        'Include the standard console and string headers.',
        'Keep the program inside an int main() frame that returns 0.',
        'Keep points_per_detail set to the integer 5.',
        'Display Welcome to the Observation Desk! on its own line.',
        'Ask for the observer name and read the complete line into observer_name.',
        'Ask for the detail count and read a whole number into details.',
        'Calculate focus_points by multiplying details and points_per_detail.',
        'Build one std::cout report containing observer_name, details, and focus_points.',
      ],
      exercise: {
        id: 'project-cpp-final',
        conceptId: 'project-cpp-final-compiled-program',
        eyebrow: 'Checkpoint 12 of 12',
        title: 'Build the Observation Desk',
        explanation:
          'This final source file combines headers, the main frame, typed variables, console output, full-line text input, integer extraction, multiplication, and one personal report. The compiler must build it before any test can run.',
        analogy:
          'You now have the complete desk workflow: open the desk, record the observer, count the details, score the observation, and file a report. Each station passes a value to the next one.',
        type: 'code',
        prompt: 'Complete the Observation Desk from the written requirements. The visible example uses Avery and 2, while official checks use other values.',
        starterCode: '#include <iostream>\n#include <string>\n\nint main() {\n    int points_per_detail = 5;\n\n    // Display the welcome line.\n    // Ask for the observer name and read the full line.\n    // Ask for the detail count and read a whole number.\n    // Calculate the focus points.\n    // Print the personal observation report.\n\n    return 0;\n}',
        focus: 'Write the missing C++ statements as complete lines. There are no _____ blanks, and the official input values will change.',
        codeGuide: [
          { code: 'std::cout << "message\\n";', plain: 'Use the familiar output-stream shape for each fixed welcome or question line.' },
          { code: 'std::string name; std::getline(std::cin, name);', plain: 'Declare text storage, then read a complete input line so names containing spaces stay together.' },
          { code: 'int count = 0; std::cin >> count;', plain: 'Prepare an integer, then extract the typed whole-number input directly into that variable.' },
          { code: 'int result = amount * rule;', plain: 'Calculate from variables instead of memorizing the visible result, because official input values will change.' },
          { code: 'std::cout << value << " text";', plain: 'Chain fixed strings and all three required live values into the final personal report.' },
          { code: 'Run before Check', plain: 'Experiment with the visible input first. Then use the official check for additional server-owned cases and code requirements.' },
        ],
        checks: [
          { pattern: '#include\\s*<iostream>', message: 'Keep the iostream header for std::cout and std::cin.' },
          { pattern: '#include\\s*<string>', message: 'Keep the string header for std::string and full-name storage.' },
          { pattern: 'int\\s+points_per_detail\\s*=\\s*5\\s*;', message: 'Keep points_per_detail initialized to the integer 5.' },
          { pattern: 'std::string\\s+observer_name\\s*;', message: 'Declare observer_name as a std::string.' },
          { pattern: 'std::getline\\s*\\(\\s*std::cin\\s*,\\s*observer_name\\s*\\)\\s*;', message: 'Read the complete name line into observer_name with std::getline.' },
          { pattern: 'int\\s+details\\s*=\\s*0\\s*;', message: 'Declare details as an int with a safe starting value.' },
          { pattern: 'std::cin\\s*>>\\s*details\\s*;', message: 'Read the whole-number count into details.' },
          { pattern: 'int\\s+focus_points\\s*=\\s*details\\s*\\*\\s*points_per_detail\\s*;', message: 'Calculate focus_points by multiplying details and points_per_detail.' },
          { pattern: 'std::cout\\s*<<[^;]*observer_name[^;]*details[^;]*focus_points[^;]*;', message: 'Build one report chain containing observer_name, details, and focus_points.' },
          { pattern: 'return\\s+0\\s*;', message: 'Keep return 0; at the end of main to report a normal finish.' },
        ],
        output: finalProjectVisibleTestCase.expectedStdout,
        hint: 'Build from checkpoint 11, then add the fixed welcome line before the two questions. Keep every result driven by the input variables.',
        recap: 'You built C++ source that compiles, listens, stores typed values, calculates, and reports. Different input now produces a different observation result.',
        xp: 40,
      },
      practiceStdin: finalProjectVisibleTestCase.stdin,
      assessmentSummary: {
        visibleTestCase: finalProjectVisibleTestCase,
        hiddenTestCount: 3,
        structuralCheckCount: 8,
      },
    },
  ],
} satisfies CppCompiledProject
