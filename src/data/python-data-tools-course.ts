import type { Exercise, Mission } from '../types'

const returnValueExercises: Exercise[] = [
  {
    id: 'pydata1-retrieve-call',
    conceptId: 'python-parameters-and-calls',
    eyebrow: 'Memory check 1 of 5',
    title: 'Trace a familiar function call',
    explanation:
      'A function definition names a reusable job. Its parameter is a temporary input name, and a later call supplies the argument value used for that run.',
    analogy:
      'Think of a label printer with one input slot. The call places markers into the slot, and the printer uses that value while it performs its saved job.',
    type: 'prediction',
    prompt: 'What line will this familiar function call display?',
    displayCode: 'def label_item(item):\n    print("Item:", item)\n\nlabel_item("markers")',
    choices: [
      { id: 'a', label: 'Item: markers', detail: 'The argument "markers" becomes the parameter item during this call.' },
      { id: 'b', label: 'Item: item', detail: 'The parameter name retrieves its current argument value instead of displaying its own label.' },
      { id: 'c', label: 'Nothing', detail: 'The final line calls the function, so its indented print instruction runs.' },
    ],
    correctChoice: 'a',
    output: 'Item: markers',
    hint: 'Move the argument "markers" into the temporary parameter named item, then read the print line.',
    recap: 'A function call supplies an argument, and the function uses that value through its parameter.',
    xp: 8,
  },
  {
    id: 'pydata1-return-purpose',
    conceptId: 'python-return-values',
    eyebrow: 'Function workshop 2 of 5',
    title: 'Meet return',
    explanation:
      'The return keyword sends a value back to the code that called a function. That returned value can be stored, calculated with, compared, or passed to another function.',
    analogy:
      'A calculator does not only show its inner work. It hands one finished answer back so the next person can record or reuse it.',
    type: 'choice',
    prompt: 'What job does return perform inside a function?',
    choices: [
      { id: 'a', label: 'Sends a value back to the caller', detail: 'The calling code can then store or reuse that result.' },
      { id: 'b', label: 'Always displays a console line', detail: 'Displaying is the job of print. A returned value can be used without being displayed.' },
      { id: 'c', label: 'Repeats the function forever', detail: 'A call runs the function once unless other control flow says otherwise.' },
    ],
    correctChoice: 'a',
    hint: 'Choose the answer about giving one finished result back to the calling code.',
    recap: 'return gives a function result back to its caller. print displays a value but does not hand that displayed value back.',
    xp: 10,
  },
  {
    id: 'pydata1-predict-result',
    conceptId: 'python-return-values',
    eyebrow: 'Result path 3 of 5',
    title: 'Use a returned result',
    explanation:
      'A function call is an expression when it produces a returned value. Python finishes the call first, then stores its result in the variable on the left of the equals sign.',
    analogy:
      'The doubling station finishes its calculation before the warehouse writes the answer on the box labeled units.',
    type: 'prediction',
    prompt: 'What number reaches the console?',
    displayCode: 'def double(count):\n    return count * 2\n\nunits = double(4)\nprint(units)',
    choices: [
      { id: 'a', label: '8', detail: 'double(4) returns 4 times 2, and units stores that result.' },
      { id: 'b', label: '4', detail: 'Four enters through count, but the function returns the calculated value.' },
      { id: 'c', label: 'count * 2', detail: 'Python evaluates the multiplication rather than displaying the written expression.' },
    ],
    correctChoice: 'a',
    output: '8',
    hint: 'Run the function in your head first: count is 4, so what does count * 2 produce?',
    recap: 'A returned value can become the value of an assignment such as units = double(4).',
    xp: 14,
  },
  {
    id: 'pydata1-fix-return',
    conceptId: 'python-return-values',
    eyebrow: 'Debugging bench 4 of 5',
    title: 'Repair the subtotal result',
    explanation:
      'This function calculates the correct subtotal and stores it in total, but its return line sends the original price back instead. The caller therefore receives the wrong value.',
    analogy:
      'A receipt clerk calculates the full order correctly, then accidentally hands the customer the price of one item. The repair is on the handoff line.',
    type: 'bugfix',
    prompt: 'Repair the return line so the function sends back the calculated subtotal.',
    starterCode: 'def subtotal(price, quantity):\n    total = price * quantity\n    return price\n\nprint(subtotal(4, 3))',
    focus: 'Change return price to return total. Keep the calculation and supplied call in place.',
    codeGuide: [
      { code: 'total = price * quantity', plain: 'This calculates the subtotal and stores the finished number under the clear local name total.' },
      { code: 'return price', plain: 'This is the faulty handoff. It sends back only the price of one item instead of the value that was just calculated.' },
      { code: 'return total', plain: 'This repaired line sends the calculated subtotal back to the print call outside the function.' },
    ],
    checks: [
      { pattern: 'return\\s+total(?:\\s|$)', message: 'Return the calculated variable named total, not the original price parameter.' },
      { pattern: 'total\\s*=\\s*price\\s*\\*\\s*quantity', message: 'Keep the supplied multiplication that calculates price times quantity.' },
    ],
    output: '12',
    hint: 'The correct result is already stored under total. Return that variable by name.',
    recap: 'Return the variable that holds the completed result so the caller receives the value the function was designed to calculate.',
    xp: 16,
  },
  {
    id: 'pydata1-subtotal',
    conceptId: 'python-returned-calculations',
    eyebrow: 'Reusable calculation 5 of 5',
    title: 'Build a reusable subtotal',
    explanation:
      'A useful calculation function receives the values it needs and returns one answer. Different calls can reuse the same function with different prices and quantities.',
    analogy:
      'One checkout rule can calculate many orders. The cashier changes the numbers placed into the two input slots instead of rebuilding the rule.',
    type: 'code',
    prompt: 'Complete the function so both calls receive the correct subtotal.',
    starterCode: 'def subtotal(price, quantity):\n    _____\n\nfirst_total = subtotal(4, 3)\nsecond_total = subtotal(2, 5)\n\nprint(first_total)\nprint(second_total)',
    focus: 'Replace the one _____ blank with return price * quantity as one complete Python line.',
    codeGuide: [
      { code: 'price, quantity', plain: 'These two parameters receive the two numbers supplied by each function call.' },
      { code: 'price * quantity', plain: 'The multiplication expression produces the subtotal for the current call.' },
      { code: 'return price * quantity', plain: 'return sends the calculated number back so the assignment outside the function can store it.' },
      { code: 'subtotal(4, 3)', plain: 'This call uses 4 as price and 3 as quantity. The later call reuses the same job with 2 and 5.' },
    ],
    checks: [
      { pattern: 'def\\s+subtotal\\s*\\(\\s*price\\s*,\\s*quantity\\s*\\)\\s*:', message: 'Keep the subtotal function with its price and quantity parameters.' },
      { pattern: 'return\\s+price\\s*\\*\\s*quantity', message: 'Return the result of multiplying price by quantity.' },
    ],
    output: '12\n10',
    hint: 'The complete indented line is return price * quantity.',
    recap: 'A returned calculation can be stored and reused. The same function can produce correct results for different arguments.',
    xp: 22,
  },
]

const textCleanupExercises: Exercise[] = [
  {
    id: 'pydata2-retrieve-format',
    conceptId: 'project-python-f-strings',
    eyebrow: 'Project recall 1 of 5',
    title: 'Recall an f-string report',
    explanation:
      'An f-string begins with the letter f before its opening quotation mark. Braces inside the string retrieve values and place them into the finished text.',
    analogy:
      'A report template keeps the fixed words in place and opens labeled windows where the current item and count should appear.',
    type: 'prediction',
    prompt: 'What line will this familiar f-string display?',
    displayCode: 'item = "markers"\ncount = 4\nprint(f"{item}: {count}")',
    choices: [
      { id: 'a', label: 'markers: 4', detail: 'Each pair of braces inserts the current value of its variable.' },
      { id: 'b', label: '{item}: {count}', detail: 'The f prefix tells Python to replace the fields instead of leaving the braces as ordinary text.' },
      { id: 'c', label: 'item: count', detail: 'The braces retrieve the stored values rather than the variable names.' },
    ],
    correctChoice: 'a',
    output: 'markers: 4',
    hint: 'Replace {item} with markers and {count} with 4, then read the completed line.',
    recap: 'An f-string inserts current values into text by placing expressions inside braces.',
    xp: 8,
  },
  {
    id: 'pydata2-strip-purpose',
    conceptId: 'python-string-methods',
    eyebrow: 'Text workshop 2 of 5',
    title: 'Trim extra spaces',
    explanation:
      'A method is an operation attached to a value. The string method strip returns text without whitespace at its beginning or end, while leaving spaces inside the text alone.',
    analogy:
      'A label trimmer cuts empty margin from the two outer edges but does not remove the spaces between words printed in the middle.',
    type: 'choice',
    prompt: 'What does name.strip() return?',
    choices: [
      { id: 'a', label: 'The text without surrounding whitespace', detail: 'Whitespace at the beginning and end is removed.' },
      { id: 'b', label: 'The text with every internal space removed', detail: 'strip leaves spaces between words intact.' },
      { id: 'c', label: 'A number showing the text length', detail: 'The built-in len operation reports length. strip returns cleaned text.' },
    ],
    correctChoice: 'a',
    hint: 'Think about trimming only the empty margin around the outside of a label.',
    recap: 'string.strip() returns a string with leading and trailing whitespace removed.',
    xp: 10,
  },
  {
    id: 'pydata2-predict-cleanup',
    conceptId: 'python-string-normalization',
    eyebrow: 'Cleanup path 3 of 5',
    title: 'Normalize the same name',
    explanation:
      'Methods can be chained when one returned value becomes the starting value for the next method. Python strips the outer spaces first, then lower changes the remaining letters to lowercase.',
    analogy:
      'A label passes through two stations in order. The first trims its edges, and the second applies one consistent lettering style.',
    type: 'prediction',
    prompt: 'What text reaches the console after both cleanup steps?',
    displayCode: 'name = " Markers "\nclean_name = name.strip().lower()\nprint(clean_name)',
    choices: [
      { id: 'a', label: 'markers', detail: 'strip removes the outer spaces, then lower changes the capital M.' },
      { id: 'b', label: ' Markers ', detail: 'That is the original text before either method returns a cleaned value.' },
      { id: 'c', label: 'MARKERS', detail: 'The lower method produces lowercase letters, not uppercase letters.' },
    ],
    correctChoice: 'a',
    output: 'markers',
    hint: 'Read the chain from left to right: remove outer spaces, then change letters to lowercase.',
    recap: 'name.strip().lower() returns consistently lowercase text without surrounding whitespace.',
    xp: 14,
  },
  {
    id: 'pydata2-fix-method-call',
    conceptId: 'python-string-normalization',
    eyebrow: 'Text repair 4 of 5',
    title: 'Call the cleanup method',
    explanation:
      'The name lower refers to a method, but parentheses are what call that method. Without the parentheses, clean_name stores the operation itself instead of its lowercase text result.',
    analogy:
      'Pointing at the label machine is not the same as pressing its button. The empty parentheses perform that button press.',
    type: 'bugfix',
    prompt: 'Repair the final cleanup step so clean_name stores lowercase text.',
    starterCode: 'item_name = " Markers "\nclean_name = item_name.strip().lower\n\nprint(clean_name)',
    focus: 'Add empty parentheses after lower so the line calls lower(). Leave strip() and the print instruction in place.',
    codeGuide: [
      { code: 'item_name.strip()', plain: 'This first method call returns the text after removing whitespace from its outer edges.' },
      { code: '.lower', plain: 'This refers to the lowercase operation but does not run it because the call parentheses are missing.' },
      { code: '.lower()', plain: 'The parentheses call the method and return the lowercase text that clean_name should store.' },
    ],
    checks: [
      { pattern: 'item_name\\.strip\\s*\\(\\s*\\)\\.lower\\s*\\(\\s*\\)', message: 'Call both cleanup methods with parentheses: item_name.strip().lower().' },
      { pattern: 'print\\s*\\(\\s*clean_name\\s*\\)', message: 'Keep print(clean_name) so the corrected text reaches the console.' },
    ],
    output: 'markers',
    hint: 'The corrected expression ends with .lower(), including both parentheses.',
    recap: 'Writing parentheses after a method name calls the method and produces its returned value.',
    xp: 16,
  },
  {
    id: 'pydata2-normalize-name',
    conceptId: 'python-normalization-functions',
    eyebrow: 'Reusable cleanup 5 of 5',
    title: 'Build normalize_name',
    explanation:
      'A normalization function applies the same cleanup rule every time. It receives text through a parameter and returns one consistent version for the rest of the program to use.',
    analogy:
      'Every incoming label passes through the same preparation desk, so Paper, PAPER, and text with extra edge spaces follow one dependable standard.',
    type: 'code',
    prompt: 'Complete normalize_name so both calls return clean lowercase text.',
    starterCode: 'def normalize_name(name):\n    _____\n\nprint(normalize_name(" Markers "))\nprint(normalize_name("PAPER"))',
    focus: 'Replace the one _____ blank with return name.strip().lower() as one complete indented line.',
    codeGuide: [
      { code: 'def normalize_name(name):', plain: 'This defines a reusable cleanup job with one temporary input parameter named name.' },
      { code: 'name.strip()', plain: 'The first method returns the input without surrounding whitespace.' },
      { code: '.lower()', plain: 'The next method returns the already-trimmed text with lowercase letters.' },
      { code: 'return ...', plain: 'return hands the cleaned text back to whichever call asked the function to normalize a name.' },
    ],
    checks: [
      { pattern: 'def\\s+normalize_name\\s*\\(\\s*name\\s*\\)\\s*:', message: 'Keep the normalize_name function with its name parameter.' },
      { pattern: 'return\\s+name\\.strip\\s*\\(\\s*\\)\\.lower\\s*\\(\\s*\\)', message: 'Return name.strip().lower() so both cleanup steps run in order.' },
    ],
    output: 'markers\npaper',
    hint: 'The complete indented line is return name.strip().lower().',
    recap: 'A normalization function returns one consistent text form that later parts of a program can store and compare safely.',
    xp: 22,
  },
]

const listToolExercises: Exercise[] = [
  {
    id: 'pydata3-retrieve-loop',
    conceptId: 'python-loops-and-collections',
    eyebrow: 'List recall 1 of 5',
    title: 'Recall a loop over a list',
    explanation:
      'A for loop visits every value in a list in order. During each pass, the loop variable holds the current value while the indented body performs its repeated job.',
    analogy:
      'A reviewer moves down a checklist one entry at a time. The same inspection rule runs for whichever entry is currently under the pointer.',
    type: 'prediction',
    prompt: 'What two lines will this familiar loop display?',
    displayCode: 'tasks = ["email", "backup"]\n\nfor task in tasks:\n    print("Task:", task)',
    choices: [
      { id: 'a', label: 'Task: email, then Task: backup', detail: 'The loop visits both list values in their stored order.' },
      { id: 'b', label: 'Task: task twice', detail: 'The loop variable retrieves each current value instead of displaying its own name.' },
      { id: 'c', label: 'Only Task: email', detail: 'The loop continues until it has visited every value in the list.' },
    ],
    correctChoice: 'a',
    output: 'Task: email\nTask: backup',
    hint: 'Trace one pass with task holding email, then one pass with task holding backup.',
    recap: 'A for loop visits each list value and runs its indented body once per value.',
    xp: 8,
  },
  {
    id: 'pydata3-append-purpose',
    conceptId: 'python-list-mutation',
    eyebrow: 'Changing lists 2 of 5',
    title: 'Add one item with append',
    explanation:
      'The list method append adds one value to the end of an existing list. Changing an existing value in place is called mutation, which simply means the stored collection now has different contents.',
    analogy:
      'A clipboard already holds two tasks. append writes one new task on the next open line instead of replacing the whole clipboard.',
    type: 'choice',
    prompt: 'What does tasks.append("report") do?',
    choices: [
      { id: 'a', label: 'Adds report to the end of tasks', detail: 'The existing list keeps its earlier values and gains one more.' },
      { id: 'b', label: 'Replaces every task with report', detail: 'append keeps the existing entries rather than clearing them.' },
      { id: 'c', label: 'Only displays report', detail: 'append changes the list. It does not print anything by itself.' },
    ],
    correctChoice: 'a',
    hint: 'Choose the answer about keeping the old entries and adding one value after them.',
    recap: 'list.append(value) mutates a list by adding one value at its end.',
    xp: 10,
  },
  {
    id: 'pydata3-predict-length',
    conceptId: 'python-list-length',
    eyebrow: 'Current count 3 of 5',
    title: 'Count a changed list',
    explanation:
      'The built-in len function returns the current number of items in a collection. Because append changes the list first, len sees the new item when it runs afterward.',
    analogy:
      'The clerk adds one card to a two-card tray, then counts the cards that are in the tray now. The current count is three.',
    type: 'prediction',
    prompt: 'What number will len(tasks) return after the append call?',
    displayCode: 'tasks = ["email", "backup"]\ntasks.append("report")\nprint(len(tasks))',
    choices: [
      { id: 'a', label: '3', detail: 'The list begins with two values and append adds one more.' },
      { id: 'b', label: '2', detail: 'That was the length before report was appended.' },
      { id: 'c', label: 'report', detail: 'len returns a numeric count rather than one of the list values.' },
    ],
    correctChoice: 'a',
    output: '3',
    hint: 'Count email, backup, and the newly appended report.',
    recap: 'len(list_name) returns the number of items currently stored in that list.',
    xp: 14,
  },
  {
    id: 'pydata3-fix-membership',
    conceptId: 'python-membership-tests',
    eyebrow: 'Membership repair 4 of 5',
    title: 'Repair the membership test',
    explanation:
      'The in operator asks whether one value appears inside a collection. Comparing task with the entire tasks list using == asks a different question and is false here.',
    analogy:
      'The useful question is whether the backup card is somewhere on the clipboard, not whether one card is equal to the whole clipboard.',
    type: 'bugfix',
    prompt: 'Repair the condition so it checks whether task appears inside tasks.',
    starterCode: 'tasks = ["email", "backup"]\ntask = "backup"\n\nif task == tasks:\n    print("Already listed")\nelse:\n    print("Missing")',
    focus: 'Change task == tasks to task in tasks. Keep both decision routes and the supplied values unchanged.',
    codeGuide: [
      { code: 'task == tasks', plain: 'This faulty comparison asks whether one string is equal to the complete list, which is not the intended question.' },
      { code: 'task in tasks', plain: 'The in operator asks whether the value currently stored in task appears anywhere in the tasks list.' },
      { code: 'if ...:', plain: 'The if route runs when the membership question is True. The supplied backup value makes it True.' },
    ],
    checks: [
      { pattern: 'if\\s+task\\s+in\\s+tasks\\s*:', message: 'Use task in tasks as the complete if condition.' },
      { pattern: 'else\\s*:', message: 'Keep the supplied else route for a task that is not in the list.' },
    ],
    output: 'Already listed',
    hint: 'The repaired line is if task in tasks:',
    recap: 'Use value in collection to ask whether a collection currently contains that value.',
    xp: 16,
  },
  {
    id: 'pydata3-add-unique',
    conceptId: 'python-list-functions',
    eyebrow: 'Task helper 5 of 5',
    title: 'Add a task only once',
    explanation:
      'The not in operator is True when a value is absent. This function appends only an absent task, then returns the current list length whether or not the list changed.',
    analogy:
      'A coordinator checks the clipboard before writing. An existing task stays as one entry, while a genuinely new task receives the next line.',
    type: 'code',
    prompt: 'Complete the function so it avoids duplicates and returns the current task count.',
    starterCode: 'def add_unique(tasks, task):\n    if _____:\n        _____\n    return _____\n\ntasks = ["email", "backup"]\n\nprint(add_unique(tasks, "backup"))\nprint(add_unique(tasks, "report"))',
    focus: 'Replace the three _____ blanks with task not in tasks, tasks.append(task), and len(tasks), in that order.',
    codeGuide: [
      { code: 'task not in tasks', plain: 'This condition is True only when the proposed task does not already appear in the list.' },
      { code: 'tasks.append(task)', plain: 'This indented call adds the new task only when the condition above is True.' },
      { code: 'return len(tasks)', plain: 'This returns the current count after the optional change, so the caller receives a useful result.' },
      { code: 'two supplied calls', plain: 'The first call proves that a duplicate stays out. The second proves that a new value is appended.' },
    ],
    checks: [
      { pattern: 'if\\s+task\\s+not\\s+in\\s+tasks\\s*:', message: 'Use task not in tasks so the append runs only for a new value.' },
      { pattern: 'tasks\\.append\\s*\\(\\s*task\\s*\\)', message: 'Append the current task to the tasks list inside the if body.' },
      { pattern: 'return\\s+len\\s*\\(\\s*tasks\\s*\\)', message: 'Return len(tasks) so each caller receives the current count.' },
    ],
    output: '2\n3',
    hint: 'Read the blanks as: if the task is absent, append it, then return the list length.',
    recap: 'Membership prevents duplicate list entries, append adds a new value, and len provides the updated count.',
    xp: 22,
  },
]

const dictionaryExercises: Exercise[] = [
  {
    id: 'pydata4-retrieve-list-change',
    conceptId: 'python-list-functions',
    eyebrow: 'List helper recall 1 of 5',
    title: 'Recall a list update',
    explanation:
      'The add_unique function checks membership before changing its list. It returns the current length, so calling code can use a useful result after the optional update.',
    analogy:
      'The clipboard clerk refuses a duplicate card, accepts a new card, and reports how many cards are filed when the work is complete.',
    type: 'prediction',
    prompt: 'What two counts will these calls display?',
    displayCode: 'def add_unique(tasks, task):\n    if task not in tasks:\n        tasks.append(task)\n    return len(tasks)\n\ntasks = ["email"]\nprint(add_unique(tasks, "email"))\nprint(add_unique(tasks, "backup"))',
    choices: [
      { id: 'a', label: '1, then 2', detail: 'The duplicate changes nothing, and the new task increases the length.' },
      { id: 'b', label: '2, then 3', detail: 'The first call does not append email because it is already present.' },
      { id: 'c', label: 'email, then backup', detail: 'The function returns numeric lengths rather than the task text.' },
    ],
    correctChoice: 'a',
    output: '1\n2',
    hint: 'Trace the duplicate call first, then count again after backup is appended.',
    recap: 'A function can update a supplied list and return a separate result that describes its current state.',
    xp: 8,
  },
  {
    id: 'pydata4-dictionary-purpose',
    conceptId: 'python-dictionaries',
    eyebrow: 'Named data 2 of 5',
    title: 'Meet a dictionary',
    explanation:
      'A dictionary stores key-value pairs. Each key is a meaningful lookup label, and its paired value is the information stored under that label.',
    analogy:
      'A supply ledger uses an item name as the row label and writes the current quantity beside it. You look up markers by name, not by row number.',
    type: 'choice',
    prompt: 'Why use a dictionary for item quantities?',
    choices: [
      { id: 'a', label: 'Each item name can lead to its quantity', detail: 'Meaningful keys make direct named lookups possible.' },
      { id: 'b', label: 'A dictionary automatically saves a file', detail: 'This dictionary lives in program memory unless separate persistence code is written.' },
      { id: 'c', label: 'Every key must be a numeric index', detail: 'Dictionary keys can be descriptive strings such as "markers".' },
    ],
    correctChoice: 'a',
    hint: 'Choose the answer about using one meaningful name to find its related value.',
    recap: 'A Python dictionary maps each unique key to a value, such as an item name to its quantity.',
    xp: 10,
  },
  {
    id: 'pydata4-predict-lookup',
    conceptId: 'python-dictionary-lookup',
    eyebrow: 'Ledger lookup 3 of 5',
    title: 'Look up stock by key',
    explanation:
      'Square brackets after a dictionary name perform a key lookup. Python finds the matching key and returns the value stored with it.',
    analogy:
      'Writing markers in the ledger search box opens the markers row and reveals the quantity written beside that exact label.',
    type: 'prediction',
    prompt: 'What value does inventory["markers"] retrieve?',
    displayCode: 'inventory = {"markers": 4, "paper": 12}\nprint(inventory["markers"])',
    choices: [
      { id: 'a', label: '4', detail: 'The key "markers" is paired with the integer value 4.' },
      { id: 'b', label: 'markers', detail: 'markers is the lookup key, while 4 is its stored value.' },
      { id: 'c', label: '12', detail: 'Twelve belongs to the different key "paper".' },
    ],
    correctChoice: 'a',
    output: '4',
    hint: 'Find the key "markers" in the dictionary and read the value after its colon.',
    recap: 'dictionary[key] retrieves the value currently stored under that exact key.',
    xp: 14,
  },
  {
    id: 'pydata4-fix-missing-key',
    conceptId: 'python-dictionary-defaults',
    eyebrow: 'Missing row repair 4 of 5',
    title: 'Repair a missing-key lookup',
    explanation:
      'A bracket lookup stops with KeyError when its key is absent. The dictionary method get can return a stated default instead, which is useful when a missing quantity should begin at zero.',
    analogy:
      'The ledger has no markers row yet. Instead of treating that as a broken ledger, the clerk follows the stated rule that an unlisted item begins with zero units.',
    type: 'bugfix',
    prompt: 'Repair the lookup so a missing markers key produces 0.',
    starterCode: 'inventory = {"paper": 12}\n\nprint(inventory["markers"])',
    focus: 'Change inventory["markers"] to inventory.get("markers", 0). Keep the dictionary and print call in place.',
    codeGuide: [
      { code: 'inventory["markers"]', plain: 'This direct lookup expects the key to exist and raises KeyError because the supplied dictionary has no markers entry.' },
      { code: 'inventory.get(...)', plain: 'The get method looks for a key while allowing the program to state what should happen when that key is missing.' },
      { code: 'inventory.get("markers", 0)', plain: 'The first argument is the key to find. The second argument is the default value returned when it is absent.' },
    ],
    checks: [
      { pattern: 'inventory\\.get\\s*\\(\\s*"markers"\\s*,\\s*0\\s*\\)', message: 'Use inventory.get("markers", 0) so the missing quantity defaults to zero.' },
      { pattern: 'print\\s*\\(', message: 'Keep the supplied print call so the repaired lookup result reaches the console.' },
    ],
    output: '0',
    hint: 'The complete repaired expression is inventory.get("markers", 0).',
    recap: 'dictionary.get(key, default) safely returns the default when the requested key is not present.',
    xp: 16,
  },
  {
    id: 'pydata4-add-stock',
    conceptId: 'python-dictionary-updates',
    eyebrow: 'Stock helper 5 of 5',
    title: 'Add or update stock',
    explanation:
      'A stock update first normalizes the name, safely retrieves the current quantity, and stores the old quantity plus the new amount under the same clean key.',
    analogy:
      'The clerk standardizes the row label, reads the existing count or starts at zero, adds the delivery, and writes the new count back on that row.',
    type: 'code',
    prompt: 'Complete add_stock so differently formatted names update one dictionary entry.',
    starterCode: 'def normalize_name(name):\n    return name.strip().lower()\n\ndef add_stock(inventory, name, amount):\n    clean_name = _____\n    current = _____\n    inventory[clean_name] = _____\n    return inventory[clean_name]\n\ninventory = {}\nprint(add_stock(inventory, " Markers ", 2))\nprint(add_stock(inventory, "markers", 3))',
    focus: 'Replace the three _____ blanks with normalize_name(name), inventory.get(clean_name, 0), and current + amount.',
    codeGuide: [
      { code: 'clean_name = normalize_name(name)', plain: 'This reuses the text cleanup function so surrounding spaces and letter case do not create accidental extra keys.' },
      { code: 'inventory.get(clean_name, 0)', plain: 'This retrieves the current quantity or returns zero when the clean key has not been stored yet.' },
      { code: 'inventory[clean_name] = current + amount', plain: 'This dictionary assignment writes the updated quantity under the same normalized key.' },
      { code: 'return inventory[clean_name]', plain: 'The function returns the newly stored quantity so the caller can use or report the result.' },
    ],
    checks: [
      { pattern: 'clean_name\\s*=\\s*normalize_name\\s*\\(\\s*name\\s*\\)', message: 'Store normalize_name(name) in clean_name before using the dictionary.' },
      { pattern: 'current\\s*=\\s*inventory\\.get\\s*\\(\\s*clean_name\\s*,\\s*0\\s*\\)', message: 'Retrieve the current clean_name quantity with a default of zero.' },
      { pattern: 'inventory\\s*\\[\\s*clean_name\\s*\\]\\s*=\\s*current\\s*\\+\\s*amount', message: 'Store current + amount under inventory[clean_name].' },
      { pattern: 'return\\s+inventory\\s*\\[\\s*clean_name\\s*\\]', message: 'Keep the return of the newly updated dictionary value.' },
    ],
    output: '2\n5',
    hint: 'Clean the name, get its current count or zero, then store current + amount.',
    recap: 'A safe dictionary update normalizes its key, retrieves a default when needed, stores the new value, and returns the result.',
    xp: 22,
  },
]

const summaryExercises: Exercise[] = [
  {
    id: 'pydata5-retrieve-update',
    conceptId: 'python-dictionary-updates',
    eyebrow: 'Dictionary recall 1 of 5',
    title: 'Recall a safe stock update',
    explanation:
      'A safe dictionary update retrieves an existing value with a default, calculates the replacement value, and stores that result under the same key.',
    analogy:
      'The ledger clerk reads two units on the markers row, adds a delivery of three, and replaces the old count with five.',
    type: 'prediction',
    prompt: 'What quantity will this update display?',
    displayCode: 'inventory = {"markers": 2}\ncurrent = inventory.get("markers", 0)\ninventory["markers"] = current + 3\nprint(inventory["markers"])',
    choices: [
      { id: 'a', label: '5', detail: 'The existing 2 and the delivered 3 are added before the dictionary is updated.' },
      { id: 'b', label: '3', detail: 'Three is the added amount, not the complete updated quantity.' },
      { id: 'c', label: '0', detail: 'The default is not used because the markers key already exists.' },
    ],
    correctChoice: 'a',
    output: '5',
    hint: 'The get call finds 2, then the assignment stores 2 + 3.',
    recap: 'Read the current value, calculate the replacement, and assign it back to update a dictionary entry.',
    xp: 8,
  },
  {
    id: 'pydata5-accumulator-purpose',
    conceptId: 'python-accumulators',
    eyebrow: 'Running totals 2 of 5',
    title: 'Grow one running total',
    explanation:
      'An accumulator is a variable that keeps a result as a loop visits more values. The instruction total += amount means the same as total = total + amount.',
    analogy:
      'A cashier starts a receipt total at zero and adds each item amount to the number already written at the bottom.',
    type: 'choice',
    prompt: 'What job does total += amount perform inside a loop?',
    choices: [
      { id: 'a', label: 'Adds the current amount to the running total', detail: 'The old total and current amount become the next total.' },
      { id: 'b', label: 'Resets total to zero', detail: 'The zero setup should happen once before the loop begins.' },
      { id: 'c', label: 'Turns total into text', detail: 'Both values remain numbers during this addition.' },
    ],
    correctChoice: 'a',
    hint: 'Read += as “take the old value on the left and add the value on the right.”',
    recap: 'An accumulator starts once and grows during each relevant loop pass with an update such as total += amount.',
    xp: 10,
  },
  {
    id: 'pydata5-order-total',
    conceptId: 'python-dictionary-iteration',
    eyebrow: 'Summary plan 3 of 5',
    title: 'Put a dictionary total in order',
    explanation:
      'The values method provides the stored dictionary values without their keys. A running total must begin before the loop, then add each current value inside the loop before it is printed.',
    analogy:
      'Open the ledger first, write a starting zero once, visit every quantity row, and report the finished total only after the final row.',
    type: 'ordering',
    prompt: 'Arrange the pieces into a complete inventory total.',
    orderItems: [
      { id: 'add', code: '    total += amount' },
      { id: 'report', code: 'print(total)' },
      { id: 'inventory', code: 'inventory = {"markers": 5, "paper": 12}' },
      { id: 'loop', code: 'for amount in inventory.values():' },
      { id: 'start', code: 'total = 0' },
    ],
    correctOrder: ['inventory', 'start', 'loop', 'add', 'report'],
    incorrectMessage: 'Create the dictionary, start total once, open the values loop, add inside it, then report after the loop.',
    output: '17',
    hint: 'The total must exist before the loop, and print(total) belongs after every amount has been added.',
    recap: 'Use dictionary.values() to visit stored values, update the accumulator inside the loop, and report after the loop.',
    xp: 14,
  },
  {
    id: 'pydata5-fix-total-reset',
    conceptId: 'python-accumulators',
    eyebrow: 'Summary repair 4 of 5',
    title: 'Move the reset out of the loop',
    explanation:
      'This program sets total back to zero during every loop pass. Each reset discards the earlier work, so the final result contains only the last amount.',
    analogy:
      'A cashier erases the receipt total before adding every new item. The fix is to write zero once before scanning begins.',
    type: 'bugfix',
    prompt: 'Move total = 0 before the loop so both quantities contribute to the result.',
    starterCode: 'inventory = {"markers": 5, "paper": 12}\n\nfor amount in inventory.values():\n    total = 0\n    total += amount\n\nprint(total)',
    focus: 'Move the complete line total = 0 above the for line. Keep total += amount indented inside the loop.',
    codeGuide: [
      { code: 'total = 0', plain: 'This creates the starting accumulator. It should run once before any dictionary value is visited.' },
      { code: 'for amount in inventory.values():', plain: 'This loop gives amount each stored quantity, first 5 and then 12.' },
      { code: 'total += amount', plain: 'This indented update keeps the earlier sum and adds the current amount during each pass.' },
      { code: 'print(total)', plain: 'This unindented report runs after the loop and displays the finished sum.' },
    ],
    checks: [
      { pattern: 'total\\s*=\\s*0[\\s\\S]*for\\s+amount\\s+in\\s+inventory\\.values\\s*\\(\\s*\\)\\s*:', message: 'Place total = 0 before the loop over inventory.values().' },
      { pattern: 'for\\s+amount[\\s\\S]*\\n\\s+total\\s*\\+=\\s*amount', message: 'Keep total += amount indented inside the loop body.' },
    ],
    output: '17',
    hint: 'Cut total = 0 from the loop body and place it on its own line immediately before for amount in inventory.values():',
    recap: 'Initialize an accumulator once before a loop, update it during each pass, and use the completed result afterward.',
    xp: 16,
  },
  {
    id: 'pydata5-low-stock',
    conceptId: 'python-filtering-collections',
    eyebrow: 'Useful filters 5 of 5',
    title: 'Collect low-stock names',
    explanation:
      'Filtering means keeping only values that match a condition. This function visits every dictionary key and appends a name when its stored quantity is below the supplied limit.',
    analogy:
      'A stock clerk reads every ledger row but copies only the rows below the restock line onto a separate action list.',
    type: 'code',
    prompt: 'Complete the condition, append call, and returned result list.',
    starterCode: 'def low_stock(inventory, limit):\n    names = []\n    for name in inventory:\n        if _____:\n            _____\n    return _____\n\ninventory = {"markers": 5, "paper": 12, "tape": 2}\n\nfor name in low_stock(inventory, 6):\n    print(name)',
    focus: 'Replace the three _____ blanks with inventory[name] < limit, names.append(name), and names.',
    codeGuide: [
      { code: 'for name in inventory:', plain: 'Looping over a dictionary directly gives name each key in its stored order.' },
      { code: 'inventory[name] < limit', plain: 'This retrieves the current quantity and asks whether it is below the supplied restock boundary.' },
      { code: 'names.append(name)', plain: 'This runs only for a matching entry and adds its key to the result list.' },
      { code: 'return names', plain: 'After every key has been checked, the function returns the completed filtered list.' },
    ],
    checks: [
      { pattern: 'if\\s+inventory\\s*\\[\\s*name\\s*\\]\\s*<\\s*limit\\s*:', message: 'Compare inventory[name] with limit in the if condition.' },
      { pattern: 'names\\.append\\s*\\(\\s*name\\s*\\)', message: 'Append the matching name to the names result list.' },
      { pattern: 'return\\s+names', message: 'Return names after the loop has checked the complete inventory.' },
    ],
    output: 'markers\ntape',
    hint: 'Check whether inventory[name] is below limit, append name, then return names after the loop.',
    recap: 'A filter loops through available data, applies a condition, collects each match, and returns the completed result.',
    xp: 22,
  },
]

const supplyTrackerExercises: Exercise[] = [
  {
    id: 'pydata6-trace-stock-update',
    conceptId: 'python-dictionary-updates',
    eyebrow: 'System recall 1 of 5',
    title: 'Trace a normalized update',
    explanation:
      'Normalization makes differently formatted input share one dictionary key. The second update retrieves the quantity stored by the first update and adds to it instead of creating another entry.',
    analogy:
      'Two deliveries arrive with slightly different handwriting, but the receiving desk standardizes both labels and files them on the same markers row.',
    type: 'prediction',
    prompt: 'What quantity will the second call return?',
    displayCode: 'def normalize_name(name):\n    return name.strip().lower()\n\ndef add_stock(inventory, name, amount):\n    clean_name = normalize_name(name)\n    current = inventory.get(clean_name, 0)\n    inventory[clean_name] = current + amount\n    return inventory[clean_name]\n\ninventory = {}\nadd_stock(inventory, " Markers ", 2)\nprint(add_stock(inventory, "markers", 3))',
    choices: [
      { id: 'a', label: '5', detail: 'Both names normalize to markers, so the second amount is added to the first.' },
      { id: 'b', label: '3', detail: 'That would ignore the existing quantity stored under the normalized key.' },
      { id: 'c', label: '2', detail: 'Two is the result after the first delivery, before the second update.' },
    ],
    correctChoice: 'a',
    output: '5',
    hint: 'Normalize both supplied names, notice that they match, then add 2 and 3.',
    recap: 'Consistent normalized keys let repeated dictionary updates find and build on the same stored value.',
    xp: 8,
  },
  {
    id: 'pydata6-plan-tracker',
    conceptId: 'python-program-planning',
    eyebrow: 'Capstone plan 2 of 5',
    title: 'Choose each tool\'s job',
    explanation:
      'A larger program stays understandable when each part has one clear responsibility. Functions perform reusable jobs, a dictionary stores named quantities, a list collects matches, and a loop visits repeated data.',
    analogy:
      'A small workshop runs smoothly when receiving, the stock ledger, counting, and the restock list each have a clear desk instead of one desk doing everything at once.',
    type: 'choice',
    prompt: 'Which plan gives each programming tool a clear job?',
    choices: [
      { id: 'a', label: 'Normalize names, update a dictionary, total values, collect low stock', detail: 'Each step uses the focused tool learned for that responsibility.' },
      { id: 'b', label: 'Use one print call as the complete storage system', detail: 'Printing reports a value but does not organize or update the program data.' },
      { id: 'c', label: 'Create a separate variable for every possible item', detail: 'A dictionary handles changing named inventory without one variable per item.' },
    ],
    correctChoice: 'a',
    hint: 'Match text cleanup, named storage, counting, and filtering to the tools built in the earlier modules.',
    recap: 'Program planning assigns one clear responsibility to each function and data structure, then connects their results.',
    xp: 10,
  },
  {
    id: 'pydata6-order-tracker',
    conceptId: 'python-data-tool-assembly',
    eyebrow: 'Dependency order 3 of 5',
    title: 'Put the tracker together',
    explanation:
      'Python must run a function definition before later code calls that function. After the reusable jobs exist, the program can create its data, perform updates, calculate summaries, and report the results.',
    analogy:
      'Install the workshop stations first, open the ledger second, process deliveries third, and print the end-of-day report last.',
    type: 'ordering',
    prompt: 'Arrange the program sections in a dependable top-to-bottom order.',
    orderItems: [
      { id: 'report', code: 'print(total_stock(inventory))' },
      { id: 'normalize', code: 'def normalize_name(name): ...' },
      { id: 'data', code: 'inventory = {}' },
      { id: 'update', code: 'add_stock(inventory, "markers", 3)' },
      { id: 'helpers', code: 'def add_stock(...): ...\ndef total_stock(...): ...\ndef low_stock(...): ...' },
    ],
    correctOrder: ['normalize', 'helpers', 'data', 'update', 'report'],
    incorrectMessage: 'Define normalization and the helper functions first, create inventory, update it, then report from the finished data.',
    output: '3',
    hint: 'Definitions come before calls. The report needs data that has already been created and updated.',
    recap: 'Define reusable jobs before calling them, then create data, transform it, and report only after the required results exist.',
    xp: 14,
  },
  {
    id: 'pydata6-fix-normalized-key',
    conceptId: 'python-data-tool-debugging',
    eyebrow: 'Integrated repair 4 of 5',
    title: 'Repair the mixed key',
    explanation:
      'This function stores quantities under clean_name but retrieves the old quantity with the unclean name parameter. Those two key forms can disagree, so a repeated delivery can lose the earlier count.',
    analogy:
      'The clerk files deliveries under a standardized row label but searches for the old count using the handwriting from the box. The search and filing labels must match.',
    type: 'bugfix',
    prompt: 'Repair the get call so lookup and assignment use the same normalized key.',
    starterCode: 'def normalize_name(name):\n    return name.strip().lower()\n\ndef add_stock(inventory, name, amount):\n    clean_name = normalize_name(name)\n    current = inventory.get(name, 0)\n    inventory[clean_name] = current + amount\n    return inventory[clean_name]\n\ninventory = {}\nadd_stock(inventory, " Markers ", 2)\nadd_stock(inventory, "markers", 3)\nprint(inventory["markers"])',
    focus: 'Change inventory.get(name, 0) to inventory.get(clean_name, 0). Leave the normalized assignment and supplied calls unchanged.',
    codeGuide: [
      { code: 'clean_name = normalize_name(name)', plain: 'This creates the one consistent key form that the function should use for all dictionary operations.' },
      { code: 'inventory.get(name, 0)', plain: 'This faulty lookup may search with spaces or capital letters even though the stored key was normalized.' },
      { code: 'inventory.get(clean_name, 0)', plain: 'This repaired lookup searches with the exact same key form used by the assignment below.' },
      { code: 'inventory[clean_name] = current + amount', plain: 'This stores the new total under the normalized key after the matching lookup succeeds.' },
    ],
    checks: [
      { pattern: 'current\\s*=\\s*inventory\\.get\\s*\\(\\s*clean_name\\s*,\\s*0\\s*\\)', message: 'Use clean_name in the get call so lookup and assignment share one key.' },
      { pattern: 'inventory\\s*\\[\\s*clean_name\\s*\\]\\s*=\\s*current\\s*\\+\\s*amount', message: 'Keep the update stored under inventory[clean_name].' },
    ],
    output: '5',
    hint: 'The dictionary assignment uses clean_name, so the earlier get call must use clean_name too.',
    recap: 'A dictionary update must use one consistent key for retrieval and assignment or earlier values can be missed.',
    xp: 16,
  },
  {
    id: 'pydata6-supply-tracker',
    conceptId: 'python-data-tool-capstone',
    eyebrow: 'Guided capstone 5 of 5',
    title: 'Complete the Supply Tracker',
    explanation:
      'This program combines four small jobs: normalize item names, add quantities, total all stored units, and collect items below a restock limit. Complete the five familiar expressions to connect the system.',
    analogy:
      'The final workshop tool connects the receiving desk, ledger, counting station, and restock clipboard. Each station stays small, while their results form one useful report.',
    type: 'code',
    prompt: 'Complete all five blanks so the tracker combines repeated names and prints the final report.',
    starterCode: 'def normalize_name(name):\n    return name.strip().lower()\n\ndef add_stock(inventory, name, amount):\n    clean_name = _____\n    current = _____\n    inventory[clean_name] = _____\n    return inventory[clean_name]\n\ndef total_stock(inventory):\n    total = 0\n    for amount in inventory.values():\n        total += _____\n    return total\n\ndef low_stock(inventory, limit):\n    names = []\n    for name in inventory:\n        if inventory[name] < limit:\n            _____\n    return names\n\ninventory = {}\nadd_stock(inventory, " Markers ", 2)\nadd_stock(inventory, "markers", 3)\nadd_stock(inventory, "Paper", 12)\n\nprint("Products:", len(inventory))\nprint("Total units:", total_stock(inventory))\nfor name in low_stock(inventory, 6):\n    print("Restock:", name)',
    focus: 'Replace the five _____ blanks with normalize_name(name), inventory.get(clean_name, 0), current + amount, amount, and names.append(name).',
    codeGuide: [
      { code: 'normalize_name(name)', plain: 'This returns one consistent key so both markers deliveries refer to the same dictionary entry.' },
      { code: 'inventory.get(clean_name, 0)', plain: 'This reads an existing quantity or begins a new item at zero before the delivery is added.' },
      { code: 'current + amount', plain: 'This expression combines the quantity already stored with the amount from the current delivery.' },
      { code: 'total += amount', plain: 'The total_stock loop adds each dictionary value to one accumulator that began at zero.' },
      { code: 'names.append(name)', plain: 'The low_stock filter records each item name whose quantity is below the supplied limit.' },
    ],
    checks: [
      { pattern: 'clean_name\\s*=\\s*normalize_name\\s*\\(\\s*name\\s*\\)', message: 'Set clean_name to normalize_name(name).' },
      { pattern: 'current\\s*=\\s*inventory\\.get\\s*\\(\\s*clean_name\\s*,\\s*0\\s*\\)', message: 'Get the current clean_name quantity with a default of zero.' },
      { pattern: 'inventory\\s*\\[\\s*clean_name\\s*\\]\\s*=\\s*current\\s*\\+\\s*amount', message: 'Store current + amount under inventory[clean_name].' },
      { pattern: 'total\\s*\\+=\\s*amount', message: 'Add the current amount to total inside total_stock.' },
      { pattern: 'names\\.append\\s*\\(\\s*name\\s*\\)', message: 'Append each below-limit name to the names result list.' },
    ],
    output: 'Products: 2\nTotal units: 17\nRestock: markers',
    hint: 'Work from top to bottom: normalize, get the old count, add the delivery, grow the total, then append the low-stock name.',
    recap: 'You combined returned values, normalized text, changing collections, dictionary updates, totals, and filtering into one working Python data tool.',
    xp: 22,
  },
]

function courseMission(
  id: string,
  chapter: number,
  title: string,
  subtitle: string,
  description: string,
  duration: string,
  icon: Mission['icon'],
  status: Mission['status'],
  exercises: Exercise[],
): Mission {
  return {
    id,
    language: 'python',
    chapter,
    title,
    subtitle,
    description,
    duration,
    icon,
    status,
    exercises,
  }
}

export const pythonDataToolsCourse = {
  id: 'python-data-tools' as const,
  language: 'python' as const,
  missions: [
    courseMission(
      'py-data-return-values',
      1,
      'Functions That Return Answers',
      'Make reusable calculations',
      'Return values let one function hand a useful result to the next part of a program.',
      '35 min',
      'signal',
      'available',
      returnValueExercises,
    ),
    courseMission(
      'py-data-text-cleanup',
      2,
      'Cleaning and Normalizing Text',
      'Make inconsistent input dependable',
      'String methods turn extra spaces and mixed letter case into one consistent name.',
      '35 min',
      'terminal',
      'locked',
      textCleanupExercises,
    ),
    courseMission(
      'py-data-list-tools',
      3,
      'Lists That Grow and Change',
      'Add values without duplicates',
      'List methods, membership, and len support small collections that change while a program runs.',
      '35 min',
      'package',
      'locked',
      listToolExercises,
    ),
    courseMission(
      'py-data-dictionaries',
      4,
      'Dictionaries and Named Data',
      'Store quantities under useful keys',
      'Dictionary keys provide direct named lookup, safe defaults, and focused updates.',
      '35 min',
      'satellite',
      'locked',
      dictionaryExercises,
    ),
    courseMission(
      'py-data-summaries',
      5,
      'Totals and Filters',
      'Turn stored data into useful answers',
      'Accumulators and filters summarize a collection without copying one instruction per value.',
      '35 min',
      'shield',
      'locked',
      summaryExercises,
    ),
    courseMission(
      'py-data-supply-tracker',
      6,
      'Supply Tracker',
      'Guided data-tool capstone',
      'Combine normalization, returned values, changing collections, totals, and filters in one complete console tool.',
      '40 min',
      'crown',
      'locked',
      supplyTrackerExercises,
    ),
  ] satisfies Mission[],
}
